<?php
/**
 * ═══════════════════════════════════════════════════════════════════════
 *  پیکربندی فرستندهٔ پیامک — سرور ایران
 * ═══════════════════════════════════════════════════════════════════════
 *
 * این فایل را با نام «sms-poller-config.php» کنار sms-poller.php بگذارید
 * و مقدارها را پر کنید.
 *
 * ═══ هر مقدار از کجا می‌آید ═══
 *
 *   .env سرور آلمان            →   اینجا
 *   ───────────────────────────────────────────
 *   SMS_RELAY_SECRET           →   'secret'
 *   IPPANEL_KEY                →   'token'
 *   IPPANEL_FROM               →   'from'
 *   IPPANEL_PATTERN_OTP        →   'patterns' => ['otp' => ...]
 *   IPPANEL_PATTERN_WELCOME    →   'patterns' => ['welcome' => ...]
 *   IPPANEL_PATTERN_INVOICE    →   'patterns' => ['invoice' => ...]
 *   … و به همین ترتیب
 *
 * یعنی اسم متغیر بعد از «IPPANEL_PATTERN_» با حروف کوچک، می‌شود کلید
 * داخل patterns.
 *
 * بعد از اینکه این فایل را ساختید، می‌توانید IPPANEL_KEY و همهٔ
 * IPPANEL_PATTERN_* را از .env سرور آلمان **بردارید** — دیگر لازم نیستند
 * و بهتر است کلید پیامک روی سرور خارج از ایران نماند.
 *
 * ⚠ این فایل کلید API دارد. یک .htaccess کنارش بگذارید:
 *
 *     <FilesMatch "(secret|config)\.php$">
 *       Require all denied
 *     </FilesMatch>
 */

return [

    /*
    | آدرس پل روی سرور آلمان — بدون اسلش انتهایی.
    */
    'bridge' => 'https://servernet.cloud/api/sms',

    /*
    | همان رشته‌ای که در .env سرور آلمان با نام SMS_RELAY_SECRET گذاشته‌اید.
    | دو طرف باید دقیقاً یکی باشند، وگرنه هر درخواست با «bad_signature» رد
    | می‌شود.
    */
    'secret' => 'اینجا SMS_RELAY_SECRET را بگذارید',

    /*
    | کلید API آی‌پی‌پنل — همان IPPANEL_KEY.
    */
    'token' => 'اینجا کلید آی‌پی‌پنل را بگذارید',

    /*
    | خط خدماتی شما — همان IPPANEL_FROM.
    | هر دو شکل «+983000505» و «3000505» پذیرفته است؛ خودش تبدیل می‌کند.
    */
    'from' => '+983000505',

    /*
    |------------------------------------------------------------------
    | الگوها
    |------------------------------------------------------------------
    |
    | سادهٔ‌ترین حالت — یک الگو برای کد ورود:
    |
    |     'otp' => 'abc123',
    |
    | چند الگو (هر بار یکی تصادفی انتخاب می‌شود):
    |
    |     'otp' => 'abc123, def456, ghi789',
    |
    | اگر نام متغیر داخل الگو چیزی جز «code» است، بعد از دونقطه بنویسیدش:
    |
    |     'otp' => 'abc123:code, def456:otp, ghi789:verification',
    |
    | ═══ نام متغیر را از کجا بیاورم؟ ═══
    |
    | در پنل آی‌پی‌پنل، وقتی الگو را ساختید متنش چیزی شبیه این است:
    |
    |     کد ورود شما %code% است
    |                  ↑
    |          همین کلمه، بدون درصد
    |
    | اگر نوشته‌اید «%otp%» پس بنویسید 'abc123:otp'. اگر «%code%» است،
    | لازم نیست چیزی بنویسید — پیش‌فرض همین است.
    |
    | اشتباه بودن این نام، خطای «400-2» از آی‌پی‌پنل می‌گیرد و پیامک نمی‌رود.
    |
    | رویدادی که الگو ندارد، به‌صورت پیام آزاد فرستاده می‌شود — کندتر، ولی
    | می‌رود. پس نیازی نیست همه را از همین حالا پر کنید.
    */
    'patterns' => [
        'otp' => 'اینجا کد الگوی «کد ورود» را بگذارید',

        // اینها هنوز لازم نیستند؛ هر وقت الگویشان را ساختید پر کنید
        // 'welcome'       => '',
        // 'invoice'       => '',
        // 'paid'          => '',
        // 'service_ready' => '',
        // 'expiring'      => '',
        // 'ticket_reply'  => '',
    ],
];
