CC={{ item.key }}
PORT={{ item.listener_port }}
TABLE={{ item.table }}
TUNIP={{ item.tunip }}
# audit H3: UPSTREAM (the dedicated-exit override) is NOT stored here — this base
# env is overwritten verbatim on every routing re-apply, which would wipe it. It
# lives in the drop-in /etc/servernet/country-<cc>.upstream.env, written only by
# servernet-exit-set and sourced after this file by servernet-country-run. Empty
# / absent drop-in => the free mihomo per-country pool (127.0.0.1:$PORT).
