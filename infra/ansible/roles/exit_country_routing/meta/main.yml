---
galaxy_info:
  role_name: exit_country_routing
  author: ServerNet
  description: "Module 2b Phase B: per-country tun routing + customer assignment"
  license: MIT
  min_ansible_version: "2.17"
  platforms:
    - name: Debian
      versions: [all]
# Consumes the exit_countries list defined in exit_multicountry/defaults (loaded
# play-wide in site.yml). No hard dependency so it never auto-runs another role.
dependencies: []
