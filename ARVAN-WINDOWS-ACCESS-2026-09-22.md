# اصلاح دسترسی Windows در زیرساخت ۳ — ۲۰۲۶-۰۹-۲۲

## رخداد و دامنه

- مشتری سرویس `#252` گزارش کرد سرور Windows ساخته شده، ولی راه اتصال و کنسول در پنل دیده نمی‌شود.
- بررسی زندهٔ دیتابیس نشان داد نمونهٔ ابری `#168` روی `arvan` با image key برابر `2025`، وضعیت `running` و IPv4 عمومی تحویل شده است.
- هیچ رمز رمزنگاری‌شده‌ای برای نمونه وجود نداشت و `password_seen=false` بود؛ بنابراین مشکل از سوختن نمایش یک‌بارهٔ رمز نبود.
- سرور آزمایشی ساخته نشد: رکورد زنده علت را قطعی نشان داد و ساخت آزمایشی هزینهٔ غیرضروری ایجاد می‌کرد.

## علت ریشه‌ای

1. کاتالوگ آروان Windows Server 2025 را با کلید عمومی عددی `2025` ذخیره می‌کند. صفحهٔ مشتری همهٔ نمونه‌های غیر GPU را Linux فرض می‌کرد و همیشه `root`، فرمان SSH و راهنمای Linux نشان می‌داد.
2. درایور آروان قابلیت کنسول وب ندارد و `console()` عمداً unavailable برمی‌گرداند؛ چون UI فقط قابلیت‌های مثبت را رندر می‌کرد، نبود دکمه برای مشتری بدون توضیح بود.
3. پاسخ ساخت این نمونه رمز برنگردانده بود. تلاش زنده برای `reset-root-password` روی همان نمونه با پاسخ صریح زیرساخت رد شد: تغییر رمز برای این سیستم‌عامل ممکن نیست. هیچ رمز یا دادهٔ احراز در این سند ثبت نشده است.

## اصلاح و انتشار

- commit `3403ac6` روی `develop` منتشر و روی پروداکشن مستقر شد.
- `CloudInstance::isWindows()` خانوادهٔ سیستم‌عامل را از ردیف واقعی `cloud_images` و ترکیب provider/key تشخیص می‌دهد؛ حدس از روی کلید عددی ممنوع است.
- برای Windows نام کاربر `Administrator`، فرمان `mstsc /v:<address>` و راهنمای Remote Desktop نمایش داده می‌شود؛ SSH/root دیگر نمایش داده نمی‌شود.
- اگر ارائه‌دهنده کنسول وب نداشته باشد، پنل صریح می‌گوید اتصال از راه RDP است و کنسول وب ارائه نمی‌شود.
- رشته‌ها در فارسی، انگلیسی و ترکی افزوده شدند و یک تست بازگشتی برای image key عددی `2025` اضافه شد.

## اعتبارسنجی

- هر پنج فایل PHP مستقر پیش از جایگزینی با PHP 8.4 پروداکشن lint شدند.
- cacheهای برنامه پاک و view cache با موفقیت دوباره ساخته شد (`DEPLOY_OK_3403ac6`).
- بررسی مدل زنده پیش از انقضای نشست نشان داد نمونه روشن و IP عمومی دارد، اما credential ذخیره‌شده ندارد.
- پنل خود آروان در حساب «جعفر ابراهیمی» بررسی شد. کنسول وب یا امکان reset رمز Windows در UI/API تأیید نشد؛ API reset نیز صریحاً عملیات را ناممکن اعلام کرد.

## محدودیت فعلی و اقدام لازم برای سرویس ۲۵۲

- اصلاح UI راه صحیح اتصال را نشان می‌دهد، اما برای این نمونهٔ خاص هنوز رمز Administrator موجود نیست.
- بازیابی غیرمخرب رمز از API ممکن نیست. راه باقی‌مانده نصب مجدد Windows است که کل دیسک را پاک می‌کند و باید فقط با تأیید صریح مالک/مشتری انجام شود.
- تا پیش از آن، نمونه نباید «دارای دسترسی کامل» تلقی شود، هرچند روشن و دارای IP است.

## rollback

- backup فایل‌های پیش از انتشار در `/home/servernetcloud/deploy-windows-rdp-3403ac6/backup` است.
- برای بازگشت، پنج فایل مدل/ویو/زبان از همان backup برگردانده شوند و سپس `optimize:clear` و `view:cache` اجرا شود.
- این انتشار route، NAT، firewall، شبکهٔ آروان یا خود ماشین مشتری را تغییر نداد.
