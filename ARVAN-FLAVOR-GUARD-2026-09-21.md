# گارد شناسهٔ پلن ArvanCloud — ۲۰۲۶-۰۹-۲۱

## رخداد و دامنه

- ردیاب پروداکشن دو شکست برای سرویس `#228` با پیام
  `Requested plan not found` در `09:45:16` و `10:24:05 UTC` ثبت کرد.
- سرویس مربوط لغو شده و صفحهٔ تحویل‌ها هنگام بررسی صف یا خطای بازی نداشت.
- زیرساخت ۳ فعال بود و ۳۹۳ پلن فعال در پنج شهر ایران گزارش می‌کرد؛ خرابی قطع
  کامل API یا نبود توکن نبود.

## علت ریشه‌ای

- `CloudProvisioner` فقط `provider_ref` ذخیره‌شده را به `ArvanClient` می‌داد.
- flavorهای آروان per-region هستند و شناسهٔ آن‌ها می‌تواند بین ثبت سفارش و
  پرداخت/تحویل عوض شود.
- درایور برای image تطبیق زندهٔ منطقه‌ای داشت، اما `flavor_id` را بدون بررسی
  مستقیم در درخواست ساخت می‌فرستاد.

## اصلاح

- commit: `f4680b3` روی شاخهٔ `develop`.
- `CloudProvisioner` همراه شناسه، هسته، RAM، دیسک و نوع CPU را می‌فرستد.
- `ArvanClient` پیش از ساخت، sizes همان منطقه را می‌خواند:
  - ID فعلی اگر موجود باشد بدون تغییر استفاده می‌شود؛
  - ID منقضی فقط با یک نتیجهٔ دقیقاً هم‌مشخصات جایگزین می‌شود؛
  - صفر یا چند نتیجه باعث توقف پیش از خرید و پیام قابل اقدام می‌شود.
- هیچ token، secret یا دادهٔ احراز مشتری در کد و این سند ثبت نشده است.

## اعتبارسنجی و وضعیت انتشار

- دو تست بازگشتی برای جایگزینی دقیق و توقف در تطبیق مبهم افزوده شد.
- `git diff --check` موفق بود؛ PHP روی workstation در دسترس نبود، بنابراین
  اجرای PHPUnit محلی انجام نشد.
- commit به remote پوش شد، ولی اپ پروداکشن هنوز به‌روزرسانی نشده است. cPanel
  پیام تغییر IP داد و ورود دوباره لازم دارد.
- پس از ورود و استقرار باید syntax، تست `CloudArvanTest`، پاک‌سازی cache و
  مشاهدهٔ ردیاب/صف تحویل انجام شود. ساخت آزمایشیِ واقعی بدون اجازهٔ جداگانه
  انجام نمی‌شود چون هزینه و سپس حذف منبع خارجی دارد.

## rollback

1. فایل‌های `ArvanClient.php` و `CloudProvisioner.php` از backup زمان استقرار
   بازگردانده شوند یا نسخهٔ قبل از `f4680b3` مستقر شود.
2. cacheهای config و opcode پاک شوند.
3. وضعیت زیرساخت ۳ و صف تحویل بدون ثبت خرید تازه بررسی شود.
