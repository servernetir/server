{{--
  نشان بانک.

  لوگوی واقعی بانک‌ها علامت تجاری است و در پروژه نگهداری نمی‌شود. به‌جایش
  یک نشان یکدست: مربع گرد با رنگ رسمی همان بانک و نام کوتاهش. همه یک اندازه
  و یک شکل، پس فهرست حساب‌ها مرتب دیده می‌شود — که همان خواستهٔ اصلی بود.

  اگر بانک تشخیص داده نشد، نشان خنثی با آیکن می‌نشیند تا چیدمان نشکند.
--}}
@php $b = $bank ?? null; @endphp

<span class="bank-mark" @if($b) style="--bk:{{ $b['color'] }}" @endif
      title="{{ $b['name'] ?? '' }}" aria-hidden="true">
  @if($b)
    <i>{{ $b['short'] }}</i>
  @else
    <svg class="icon"><use href="#i-db"/></svg>
  @endif
</span>
