# console.servernet.cloud — پنلِ مشتری و مدیریت. هیچ صفحه‌ای این‌جا برای
# جست‌وجو نیست و همه از قبل noindex هستند.
#
# 🔴 چرا فایلِ جدا لازم شد: کنسول همان public_html را سرو می‌کند، پس همان
# robots.txtِ سایتِ اصلی را می‌داد که با «Allow: /» شروع می‌شود. نتیجه در
# Crawl Stats (۹۰ روز): ۶۰۰ درخواستِ خزش — ۱۰٪ کلِ بودجه — صرفِ میزبانی که
# هر صفحه‌اش noindex است، در حالی که ۶۵۴ صفحهٔ بلاگ هرگز خزیده نشده بودند.
#
# ⚠️ noindex و Disallow دو کارِ متفاوت می‌کنند: اولی جلوی **ایندکس** را
# می‌گیرد، دومی جلوی **خزش** را. برای صفحه‌ای که هرگز نباید در نتایج باشد،
# هر دو درست است و آنچه بودجه را آزاد می‌کند دومی است.
#
# ⚠️ نقشهٔ سایت عمداً این‌جا اعلام نمی‌شود: نقشه مالِ میزبانِ اصلی است و
# اعلامش از میزبانِ دیگر بی‌اثر و گمراه‌کننده است.

User-agent: *
Disallow: /
