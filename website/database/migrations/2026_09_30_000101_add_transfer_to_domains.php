<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * انتقالِ دامنه از رجیسترارِ دیگر به ما.
 *
 * ═══ چرا ستونِ تازه لازم بود و `status` کافی نبود ═══
 *
 * یک ردیفِ `domains` تا امروز فقط یک داستان داشت: «خریدیم، ثبت شد». انتقال
 * داستانِ دیگری است که **هم‌زمان** با همان ردیف پیش می‌رود و سه تفاوتِ بنیادی
 * دارد:
 *
 *  ۱ **روزها طول می‌کشد، نه ثانیه‌ها.** ثبت یا در همان تماس موفق می‌شود یا
 *    شکست می‌خورد. انتقالِ gTLD پس از ثبتِ درخواست، ۵ تا ۷ روز در دستِ
 *    رجیسترارِ فعلی است و ما فقط می‌توانیم بپرسیم. پس `provision_status` که
 *    برای یک کارِ کوتاه با قفلِ ۱۵ دقیقه‌ای ساخته شده، این‌جا معنا ندارد:
 *    قفلِ رهاشده‌اش بعد از ۱۵ دقیقه باز می‌شود و کرون دوباره سفارش می‌دهد.
 *
 *  ۲ **می‌تواند از سمتِ مقابل رد شود.** رجیسترارِ فعلی یا خودِ مالک می‌تواند
 *    نه بگوید. این «خطای ما» نیست و نباید مثلِ شکستِ ثبت رفتار شود.
 *
 *  ۳ **مبلغش برمی‌گردد.** انتقالِ ردشده یعنی پولِ مشتری باید به اعتبارش
 *    برگردد؛ ثبتِ ناموفق هم همین را دارد ولی مسیرش جداست.
 *
 * ═══ 🔴 کدِ انتقال (auth code) هیچ‌جا ذخیره نمی‌شود ═══
 *
 * این تصمیم عمدی است و ستونی برایش ساخته **نشده**. آن رشته کلیدِ مالکیتِ
 * دامنه است: هرکس داشته باشد می‌تواند دامنه را از ما هم ببرد. همین پروژه در
 * `OpenProviderClient::authCode()` همین قاعده را برای کدِ **خروجی** دارد و
 * `panel_only_operations` هم صریح می‌گوید چرا.
 *
 * پس کد از فرم گرفته می‌شود، در همان درخواست به رجیسترار می‌رود، و از حافظه
 * بیرون می‌رود. اگر انتقال رد شد، از مشتری **دوباره** پرسیده می‌شود. این یک
 * ناراحتیِ کوچکِ عمدی است در برابرِ نشستنِ کلیدِ مالکیت در بکاپ‌ها و دامپ‌های
 * عیب‌یابی برای همیشه.
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('domains', function (Blueprint $table) {
            /*
            | «این ردیف از کجا آمده» — ثبتِ تازه یا انتقال.
            |
            | ⚠️ ستونِ جدا لازم است چون بعد از موفقیت، ردیفِ منتقل‌شده از ردیفِ
            | ثبت‌شده **قابلِ تشخیص نیست**: هر دو `active` با `op_id` و
            | `expires_at`. ولی گزارشِ مالی باید بداند کدام کدام است (بهای
            | انتقال با بهای ثبت فرق دارد) و پشتیبانی هم باید بداند چرا این
            | دامنه تاریخِ ثبتش قدیمی‌تر از تاریخِ ورودش به سامانهٔ ماست.
            */
            $table->string('order_type', 12)->default('register')->after('registrar');

            /*
            | چرخهٔ عمرِ انتقال، جدا از `provision_status`:
            |   null       این ردیف اصلاً انتقال نیست
            |   pending    مشتری سفارش داده، هنوز به رجیسترار نرفته
            |   submitted  به رجیسترار رفت و پذیرفته شد — حالا منتظرِ سمتِ مقابل
            |   completed  دامنه واقعاً نزدِ ما نشست
            |   rejected   رجیسترارِ فعلی/مالک رد کرد یا مهلت تمام شد
            |   failed     درخواست اصلاً ثبت نشد (کدِ غلط، دامنهٔ قفل، …)
            */
            $table->string('transfer_status', 16)->nullable()->after('order_type');
            $table->timestamp('transfer_submitted_at')->nullable()->after('transfer_status');

            /*
            | آخرین باری که وضعیت را از رجیسترار پرسیدیم.
            |
            | ⚠️ بدونِ این ستون، کرونِ پیگیری راهی ندارد بفهمد کدام ردیف را
            | تازه پرسیده و روی هر اجرا **همه** را می‌پرسد. با ده‌ها انتقالِ
            | هم‌زمان یعنی ده‌ها تماس در دقیقه به حسابی که یک بار به‌خاطرِ
            | تماسِ زیاد علامت خورده است.
            */
            $table->timestamp('transfer_checked_at')->nullable()->after('transfer_submitted_at');

            $table->index(['transfer_status', 'transfer_checked_at']);
        });
    }

    public function down(): void
    {
        Schema::table('domains', function (Blueprint $table) {
            $table->dropIndex(['transfer_status', 'transfer_checked_at']);
            $table->dropColumn(['order_type', 'transfer_status', 'transfer_submitted_at', 'transfer_checked_at']);
        });
    }
};
