<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/*
|--------------------------------------------------------------------------
| کوپنِ هدیه
|--------------------------------------------------------------------------
|
| 🔴 چرا کوپن و نه اعتبارِ کیفِ پول — و چرا این تصمیم عوض شد.
|
| نسخهٔ اول هدیه را مستقیم به کیفِ پول می‌ریخت و بعد از انقضا همان مبلغ را
| با یک ردیفِ منفی پس می‌گرفت (`min(هدیه، موجودی)`). آن قاعده فقط جلوی
| منفی‌شدنِ موجودی را می‌گرفت و **فرقِ «هدیه را خرج کرد» با «پولِ خودش را
| خرج کرد» را نمی‌فهمید**:
|
|     موجودی ۲٬۴۰۰٬۰۰۰  +  هدیه ۵۰۰٬۰۰۰   =  ۲٬۹۰۰٬۰۰۰
|     خرید سرویس ۶۰۰٬۰۰۰                  →  ۲٬۳۰۰٬۰۰۰
|     بازپس‌گیری ۵۰۰٬۰۰۰                   →  ۱٬۸۰۰٬۰۰۰
|
| یعنی مشتری برای سرویسِ ۶۰۰ هزاری عملاً ۱٬۱۰۰٬۰۰۰ می‌داد — و دقیقاً سراغِ
| کسی می‌رفت که به‌خاطرِ پیامِ ما خرید کرده بود. برنامه‌ای که برای خوش‌حالی
| ساخته شده بود به تیکتِ «چرا از حسابم کم شد» تبدیل می‌شد.
|
| کوپن این را ندارد: هیچ پولی به کیفِ پول اضافه نمی‌شود، پس چیزی هم برای
| پس‌گرفتن نیست. منقضی‌شدنش یعنی فقط استفاده نشد.
|
| ⚠️ و مالیات: کوپن روی ریلِ **پرداخت** می‌نشیند نه روی قیمت. `subtotal` و
| `tax` و `total`ِ فاکتور دست‌نخورده می‌مانند و فقط `paid` بالا می‌رود. اگر
| به‌جایش قیمت را کم می‌کردیم، پایهٔ ارزش‌افزوده هم کم می‌شد — دست‌بردن در
| چیزی که اظهارنامه‌اش جای دیگری می‌رود.
*/
return new class extends Migration
{
    public function up(): void
    {
        Schema::create('gift_coupons', function (Blueprint $table) {
            $table->id();

            /*
            | 🔴 کد **به مشتری قفل است**، و این تفاوتِ اصلی با «کد تخفیف»ِ
            | معمولی است. کدی که هرکسی بتواند بزند، همان روز در کانال‌های
            | تلگرامیِ «کد تخفیف» می‌نشیند و هدیهٔ تولدِ یک نفر خرجِ صدها
            | نفر می‌شود.
            */
            $table->foreignId('customer_id')->constrained()->cascadeOnDelete();
            $table->string('code', 24)->unique();

            $table->char('currency_code', 3)->default('IRT');
            $table->bigInteger('amount');          // واحد فرعی — برای تومان خودِ تومان

            /*
            | حداقلِ مبلغِ فاکتور برای استفاده.
            |
            | 🔴 خطِ قرمزِ کارفرما: «هرگز زیر بها نفروش». کوپنِ ۵۰۰ هزاری روی
            | فاکتورِ ۶۰۰ هزاری یعنی ۱۰۰ هزار می‌گیریم برای چیزی که چند صد
            | هزار خرج دارد. این عدد روی **خودِ کوپن** ذخیره می‌شود نه فقط در
            | تنظیمات، وگرنه تغییرِ بعدیِ تنظیمات شرطِ کوپن‌های صادرشده را
            | بازنویسی می‌کرد — و مشتری شرطی را می‌دید که موقعِ صدور نبود.
            */
            $table->bigInteger('min_invoice')->default(0);

            $table->string('reason', 32)->default('birthday');
            $table->timestamp('expires_at');

            // مصرف — `used_at` کلیدِ قفلِ اتمی است
            $table->timestamp('used_at')->nullable();
            $table->foreignId('used_invoice_id')->nullable()
                ->constrained('invoices')->nullOnDelete();

            $table->timestamps();

            /*
            | ⚠️ یکتاییِ «یک کوپن به ازای هر مشتری در هر دوره» با ایندکس
            | تضمین نمی‌شود (دوره در کد است، نه در ستون). صدورِ دوباره را
            | خودِ فرمان با پرس‌وجوی سالِ شمسی می‌بندد — همان الگوی
            | `alreadyGifted` که روی دفترِ اعتبار بود.
            */
            $table->index(['customer_id', 'used_at']);
            $table->index('expires_at');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('gift_coupons');
    }
};
