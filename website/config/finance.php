<?php

/*
| هزینهٔ واقعی هر تماس API — تومان.
|
| این اعداد پول واقعی‌اند که هر بار خرج می‌شوند. با ثبت خودکارشان در دفتر
| مالی، صاحب کسب‌وکار دقیقاً می‌داند چقدر روی احراز هویت و پیامک خرج کرده،
| نه با حدس آخر ماه.
|
| اگر تعرفهٔ سرویس‌ها عوض شد، فقط همین اعداد را به‌روز کنید.
*/

return [
    'costs' => [
        // زحل — احراز هویت ایرانی (از مستندات سرویس)
        'shahkar'          => (int) env('COST_SHAHKAR', 13000),      // تطابق کد ملی و موبایل
        'identity'         => (int) env('COST_IDENTITY', 68000),     // استعلام هویت
        'card_owner'       => (int) env('COST_CARD_OWNER', 6000),    // استعلام صاحب کارت

        // پیامک — هر پیامک موفق
        'sms'              => (int) env('COST_SMS', 1500),
    ],
];
