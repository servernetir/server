<?php

/*
|--------------------------------------------------------------------------
| اسنپ‌پی — پرداخت اقساطی
|--------------------------------------------------------------------------
|
| 🔴 هیچ اعتباری این‌جا مقدار پیش‌فرض ندارد. کلیدِ درگاه در کد یعنی کلیدی که
|    در گیت است، و کلیدی که در گیت است یعنی کلیدی که باید عوض شود.
|
| ⚠️ واحدِ مبلغ در تمامِ APIهای اسنپ‌پی **ریال** است، ولی پولِ این پروژه
|    تومان است. تبدیل فقط و فقط در SnappPayClient انجام می‌شود — همان قاعدهٔ
|    ZarinPalGateway که ضرب در ۱۰ را به یک نقطه محدود کرد.
|
| ⚠️ اسنپ‌پی هم آی‌پیِ سرور را وایت‌لیست می‌کند و هم دامنهٔ returnURL را.
|    درخواستی که از آی‌پیِ ثبت‌نشده بیاید یا returnURL‌اش با دامنهٔ ثبت‌شده
|    نخواند، رد می‌شود.
*/

return [

    'base_url' => rtrim((string) env('SNAPPPAY_BASE_URL', ''), '/'),

    'client_id'     => env('SNAPPPAY_CLIENT_ID'),
    'client_secret' => env('SNAPPPAY_CLIENT_SECRET'),
    'username'      => env('SNAPPPAY_USERNAME'),
    'password'      => env('SNAPPPAY_PASSWORD'),

    /*
    | کلیدِ روشن/خاموش. تا وقتی صریحاً true نشود، درگاه نه در فهرست می‌آید نه
    | حتی سرویسِ eligible صدا زده می‌شود — یعنی روی پروداکشن هیچ اثری ندارد
    | در حالی که کد از قبل آن‌جاست.
    */
    'enabled' => (bool) env('SNAPPPAY_ENABLED', false),

    /*
    | فهرستِ ایمیلِ حساب‌هایی که در محیطِ زنده اجازهٔ دیدنِ درگاه را دارند.
    |
    | 🔴 چون کارفرما تصمیم گرفت استیجینگ روی **همان سرورِ پروداکشن** باشد،
    |    این تنها چیزی است که بین «تستِ ما» و «مشتریِ واقعی» می‌ایستد. خالی
    |    گذاشتنش یعنی درگاه برای همه باز است.
    */
    'test_emails' => array_values(array_filter(array_map(
        'trim',
        explode(',', (string) env('SNAPPPAY_TEST_EMAILS', '')),
    ))),

    /*
    | فقط برای مشتریانی که سایت را فارسی می‌بینند و فاکتورشان تومانی است.
    | تصمیمِ کارفرما: «تمام خریدهایی که با زبان فارسی انجام می‌شود.»
    */
    'locales'    => ['fa'],
    'currencies' => ['IRT'],

    /*
    | ⚠️ افزایش اعتبارِ کیف پول عمداً بیرون است.
    |
    | خریدِ اعتبار با اقساط عملاً وام‌دادنِ نقد است و معمولاً خلافِ قواعدِ
    | درگاه‌های اعتباری؛ ضمن اینکه مشتری می‌تواند اعتبار بخرد، خرج نکند و
    | بخواهد پس بگیرد. اگر اسنپ‌پی صریحاً اجازه داد، همین یک کلید را true کنید.
    */
    'allow_topup' => (bool) env('SNAPPPAY_ALLOW_TOPUP', false),

    /*
    | دسته‌بندیِ کمیسیون. تا وقتی در قرارداد دسته‌بندیِ اختصاصی تعریف نشده،
    | مستندات می‌گوید عددِ ۱۰۰ فرستاده شود.
    */
    'commission_type' => (int) env('SNAPPPAY_COMMISSION_TYPE', 100),

    /*
    | مهلتِ پاسخ. مستندات برای verify صریحاً ۳۰ ثانیه گفته و بعدش گفته سراغِ
    | getPaymentStatus بروید — پس این عدد یک سلیقه نیست، بخشی از پروتکل است.
    */
    'timeout'        => (int) env('SNAPPPAY_TIMEOUT', 20),
    'verify_timeout' => (int) env('SNAPPPAY_VERIFY_TIMEOUT', 30),
];
