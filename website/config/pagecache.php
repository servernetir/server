<?php

/*
|--------------------------------------------------------------------------
| کشِ کاملِ صفحه — سه ممیزیِ پیاپی نبودش را علامت زدند (X-Cache: none)
|--------------------------------------------------------------------------
| نسخهٔ «کوچک‌ترینِ ممکن»ی که CTO در ممیزی ۳ خواست، ولی در لایهٔ اپ:
| روی cPanel به nginx/فایلِ کانفیگ لبه دسترسی نداریم، پس همان قرارداد
| (TTL کوتاه، BYPASS برای کاربرِ نشست‌دار، هدرِ X-Cache همیشه) این‌جا
| پیاده می‌شود. Cloudflare جلوی ما HTML را کش نمی‌کند (فقط استاتیک)، پس
| این لایه TTFB واقعی صفحات پرتردد را پایین می‌آورد.
|
| تعریفِ «انجام شد» (از خودِ ممیزی): دو `curl -I` پیاپی روی یک URL از این
| فهرست، دومی `X-Cache: HIT` بدهد؛ و صفحهٔ نشست‌دار `BYPASS`.
|
| ⚠️ فقط GETِ بی‌رشتهٔ کوئری و بی‌احراز هویت کش می‌شود؛ جزئیات و دلیلِ
| تعویضِ توکنِ CSRF در خودِ PageCache مستند است.
*/

return [
    'enabled' => env('PAGE_CACHE', true),

    // ثانیه. کوتاه عمدی است: صفحهٔ کهنه‌ی حداکثر یک‌دقیقه‌ای، در ازای حذفِ
    // رندرِ کامل برای پربازدیدترین صفحات. (همان 60sِ نسخهٔ nginx ممیزی)
    // ⚠️ و از ممیزی ۴ دیگر تنها مکانیزمِ انقضا نیست: هر ذخیرهٔ مدلِ قیمت/محتوا
    //    PageCache::purge() می‌زند و همهٔ نسخه‌ها همان لحظه باطل می‌شوند.
    'ttl' => (int) env('PAGE_CACHE_TTL', 60),

    /*
    | سقفِ نگهداریِ نسخهٔ «کهنه» (ثانیه) — همتای proxy_cache_use_stale.
    | بعد از پایانِ ttl نسخه سرو نمی‌شود ولی دور هم ریخته نمی‌شود؛ اگر رندرِ
    | تازه ۵xx داد، همین نسخهٔ سالمِ قبلی با X-Cache: STALE می‌نشیند تا مهمانِ
    | ناشناس به‌جای صفحهٔ خطا، صفحهٔ چند‌دقیقه‌پیش را ببیند.
    */
    'hard_ttl' => (int) env('PAGE_CACHE_HARD_TTL', 86400),

    /*
    |----------------------------------------------------------------------
    | حالت (ممیزی ۶): denylist — «هر بخشِ تازه به‌صورت پیش‌فرض کش می‌شود»
    |----------------------------------------------------------------------
    | ۲۲۳ صفحهٔ تازه (/parts، /urmia، /lookup، /order) از کش بیرون افتادند چون
    | در فهرستِ `routes` نبودند. حالا همه‌چیز کش می‌شود مگر آنچه این‌جا صریح
    | مستثنا شده. `allowlist` فقط برای برگشتِ اضطراری (PAGE_CACHE_MODE=allowlist).
    */
    'mode' => env('PAGE_CACHE_MODE', 'denylist'),

    // نامِ کاملِ روت (بی‌پیشوندِ زبان) که هرگز کش نمی‌شود
    'exclude_routes' => [
        'status',          // لحظه‌ای؛ کشِ ۶۰ثانیه‌ای در لحظهٔ رخداد یعنی «سبز»ِ دروغ
        'report', 'report.unsubscribe',
        'abuse', 'abuse.report', 'careers.apply', 'blog.comment',
        'share.gone', 'sharing.gone',
        'vps.index', 'hosting.index', 'domain.index',   // ریدایرکتِ ۳۰۱ — بی‌معنی برای کش
        /*
        | شورا (امنیت): صفحه‌هایی که HTMLشان به IPِ بازدیدکننده وابسته است — در HIT
        | IPِ نفرِ اول به نفرِ دوم نشان داده می‌شد. و sandboxِ سایت‌ساز (HTMLِ کاربر
        | با CSPِ خودش) جایش در کشِ عمومی نیست.
        */
        'tools', 'lookup', 'lookup.index',
        'builder.shared',
    ],

    // پیشوندِ نامِ روت — گروه‌های کاملاً خصوصی/تعاملی
    'exclude_prefixes' => [
        'account.', 'admin.', 'api.', 'auth.', 'login', 'register', 'password.',
        'system.', 'bale.', 'cloud-phone.', 'agent.', 'sms.',
        'builder.', 'payment.',
    ],

    // پیشوندِ مسیر (بی‌پیشوندِ زبان) — سپرِ دوم برای روت‌های بی‌نام
    'exclude_paths' => [
        '/account', '/admin', '/api', '/login', '/register', '/password', '/logout',
        '/system', '/cart', '/checkout', '/panel-preview', '/bale', '/cloud-phone', '/agent', '/up',
        '/sb', '/tools/ip', '/lookup', '/payment',
        // ممیزی ۷: ریدایرکتِ شمارش‌پذیرِ پرداخت و مسیرِ سلامت — هر دو باید
        // **هر بار** به PHP برسند وگرنه چیزی را نمی‌شمارند/نمی‌سنجند.
        '/go', '/healthz',
    ],

    /*
    | نامِ روتِ بدونِ پیشوندِ زبان (en./tr. خودکار پوشش داده می‌شوند، چون
    | کلیدِ کش از URL کامل ساخته می‌شود و نامِ روت فقط دروازهٔ ورود است).
    | ⚠️ فقط در حالتِ allowlist خوانده می‌شود.
    |
    | چه چیزی عمداً نیست:
    |   · status — صفحهٔ وضعیت باید لحظه‌ای باشد؛ کشِ حتی ۶۰ثانیه‌ای در
    |     لحظهٔ رخداد یعنی «سبز»ِ دروغ.
    |   · domain.search و ابزارهای lookup — تعاملی/POSTمحور.
    |   · هر مسیرِ account/admin — اصلاً به این‌جا نمی‌رسند (BYPASS نشست).
    */
    'routes' => [
        'home',
        'hosting', 'hosting.index', 'catalog',
        'cloud.index', 'cloud.location', 'vps.hourly',
        'blog.index', 'blog',
        'solutions.index', 'solution',
        'knowledge', 'docs.index', 'docs',
        'webtools.index', 'webtools',
        'servers.index', 'servers.show',
        'about', 'privacy', 'terms', 'aup', 'speed', 'sla', 'webdesign',
    ],
];
