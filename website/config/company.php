<?php

/*
|--------------------------------------------------------------------------
| هویتِ حقوقیِ شرکت — نشانه‌های اعتماد
|--------------------------------------------------------------------------
|
| 🔴 همه‌چیز این‌جا **پیش‌فرضِ خالی** دارد، و این عمدی است.
|
| ممیزیِ ده‌مدیره نتیجه گرفت گلوگاهِ سرورنت «لایهٔ اعتماد» است. ولی نشانهٔ
| اعتماد چیزی نیست که بشود نوشتش — یا شمارهٔ ثبتِ واقعی داری یا نداری. نمادِ
| جعلی از نداشتنِ نماد **بسیار** بدتر است: خریدارِ ایرانی نمادِ اعتماد را کلیک
| می‌کند و اگر به صفحهٔ نامعتبر برسد، همان لحظه کلِ سایت را کلاهبردار می‌خوانَد.
|
| پس این فایل فقط **جا** می‌سازد. هر مقداری که خالی بماند، هیچ‌جای سایت رندر
| نمی‌شود — نه کادرِ خالی، نه «۰۰۰۰۰۰»، نه «به‌زودی». دقیقاً همان قاعدهٔ
| `/status` که عمداً هیچ عددِ آپتایمِ ساختگی نمی‌سازد.
|
| ⚠️ برای پرکردن، این‌ها را در `.env` سرور بگذارید. مقدارهایی که `#` دارند را
| داخلِ گیومهٔ تک بگذارید (تلهٔ ثبت‌شده در CLAUDE.md: `#` بی‌گیومه مقدار را
| از همان‌جا می‌بُرد).
*/

return [

    /*
    | نامِ ثبت‌شدهٔ حقوقی — با نامِ تجاری فرق دارد.
    | مثال: «شرکت … (سهامی خاص)»
    */
    'legal_name' => env('COMPANY_LEGAL_NAME', ''),

    /*
    | شناسه‌های ثبتی. هرکدام که پر شود در فوتر و صفحهٔ تماس و در دادهٔ
    | ساختاریافتهٔ Organization می‌آید.
    */
    'registration_no' => env('COMPANY_REG_NO', ''),        // شماره ثبت
    'national_id'     => env('COMPANY_NATIONAL_ID', ''),   // شناسهٔ ملی
    'economic_code'   => env('COMPANY_ECONOMIC_CODE', ''), // کد اقتصادی

    /*
    | نشانیِ ثبت‌شده. برای خریدارِ ایرانی و برای گوگل هر دو مهم است: شرکتی که
    | نشانیِ فیزیکی ندارد، در نگاهِ اول «شاید فردا نباشد» خوانده می‌شود.
    */
    'address' => [
        'street'   => env('COMPANY_ADDRESS', ''),
        'city'     => env('COMPANY_CITY', ''),
        'province' => env('COMPANY_PROVINCE', ''),
        'postcode' => env('COMPANY_POSTCODE', ''),
        'country'  => env('COMPANY_COUNTRY', 'IR'),
    ],

    /*
    | سالِ تأسیس — از قبل در دادهٔ ساختاریافتهٔ صفحهٔ اصلی هست و ادعای خودِ
    | شرکت است، پس این‌جا هم همان مقدار پیش‌فرض است.
    */
    'founded' => env('COMPANY_FOUNDED', '2009'),

    /*
    |--------------------------------------------------------------------------
    | مهرهای اعتماد
    |--------------------------------------------------------------------------
    |
    | ⚠️ فقط نسخهٔ **تصویری** پشتیبانی می‌شود، و این یک تصمیمِ فنی است نه سلیقه:
    |
    |   img-src   'self' data: blob: https:      ← تصویر از هر https مجاز است ✅
    |   script-src 'self' 'unsafe-inline'        ← اسکریپتِ بیرونی بلاک است ❌
    |   frame-src 'self' https://openstreetmap   ← آی‌فریمِ بیرونی بلاک است ❌
    |
    | پس اگر کدِ اسکریپتی/آی‌فریمیِ نماد را بچسبانید، **بی‌هیچ خطایی** چیزی
    | نشان داده نمی‌شود — همان تلهٔ CSP که در CLAUDE.md ثبت است. هر دو مرجع
    | نسخهٔ تصویری هم می‌دهند؛ همان را بردارید.
    |
    | `id` و `code` را از پنلِ خودِ نماد/ساماندهی بردارید. لینک و تصویر از
    | همان‌ها ساخته می‌شود تا آدرسِ دست‌نویس اشتباه نشود.
    */
    'enamad' => [
        'id'   => env('ENAMAD_ID', ''),
        'code' => env('ENAMAD_CODE', ''),
    ],

    'samandehi' => [
        'id'   => env('SAMANDEHI_ID', ''),
        'code' => env('SAMANDEHI_CODE', ''),
    ],
];
