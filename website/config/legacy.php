<?php

/*
|--------------------------------------------------------------------------
| انتقالِ سئوی دامنهٔ قدیمی
|--------------------------------------------------------------------------
|
| `servernet.ir` بازنشسته **نمی‌شود**؛ دامنه می‌مانَد ولی صفحاتش با ۳۰۱ به
| servernet.cloud منتقل می‌شوند تا اعتبارِ سئویی‌شان هم منتقل شود.
|
| 🔴 مهم‌ترین نکتهٔ این فایل: ریدایرکت **فقط روی apex و www** است.
| زیردامنه‌های .ir زنده و حیاتی‌اند و ریدایرکتشان سرویس را می‌شکند:
|
|   • `my.servernet.ir`  → WHMCSِ زندهٔ فارسی (قیمتِ دامنه و صورت‌حساب قدیمی)
|   • `servernet.ir/sms-relay.php` → **رلهٔ پیامک**. اپراتور ایرانی آی‌پیِ
|     غیرایرانی را رد می‌کند، پس سرورِ آلمان بدونِ این رله اصلاً پیامک
|     نمی‌فرستد. ۳۰۱ روی یک POST بدنه را دور می‌ریزد ⇒ کدِ ورود گم می‌شود.
|   • `/system/health` هم همین میزبان را به‌عنوان هدفِ سلامت می‌پاید.
|
| نقشهٔ مسیرها را از دادهٔ واقعی پر کنید، نه از حدس: چند هفته با
| `unknown => 'same-path'` بالا بماند، بعد پرتکرارترین ۴۰۴ها را در
| `/admin/errors` ببینید و همان‌ها را این‌جا نگاشت کنید.
|
*/

return [

    // میزبان‌هایی که منتقل می‌شوند. زیردامنهٔ دیگری این‌جا **اضافه نکنید**.
    'hosts' => ['servernet.ir', 'www.servernet.ir'],

    /*
     | مقصد — عمداً ثابت است و از `app.url` خوانده نمی‌شود.
     |
     | `APP_URL` روی محیطِ محلی `http://localhost` است و اگر روی سرور هم
     | اشتباه ست شود، ۳۰۱ کاربران را به جای غلط می‌فرستد. این مقدار حقیقتی
     | دربارهٔ دامنهٔ ماست، نه دربارهٔ محیطِ اجرا — مثلِ `ConsoleHost::MAIN`.
     */
    'target' => 'https://servernet.cloud',

    /*
     | مسیرهایی که هرگز ریدایرکت نمی‌شوند — حتی روی apex.
     |
     | تطبیق «شروع‌شدن با» است. رلهٔ پیامک این‌جاست چون یک فایلِ PHPِ مستقل
     | روی همان میزبان است و ربطی به این اپ ندارد.
     */
    'never' => [
        '/sms-relay.php',
        '/sms-relay-secret.php',
        '/.well-known/',            // تمدیدِ گواهی SSL نباید ریدایرکت شود
    ],

    /*
     | نگاشتِ دقیقِ مسیرِ قدیمی → مسیرِ تازه (بدونِ دامنه، با اسلشِ ابتدایی).
     |
     | مثال:
     |   '/hosting-linux'  => '/hosting/linux-hosting',
     |   '/about-us'       => '/about',
     */
    'exact' => [
        '/index.php' => '/',
        '/home'      => '/',
    ],

    /*
     | نگاشتِ پیشوندی — دُمِ مسیر حفظ می‌شود.
     | `'/weblog/' => '/blog/'` یعنی `/weblog/x` → `/blog/x`.
     */
    /*
     | 🔴 عمداً **خالی** است.
     |
     | نگاشتِ حدسی بدتر از نبودِ نگاشت است. دو نمونهٔ واقعی که این‌جا بود و
     | حذف شد:
     |   • `'/category/' => '/blog'` — دسته‌بندیِ بلاگ در سایتِ تازه یک
     |     **پارامترِ کوئری** است (`/blog?cat=…`)، نه مسیر. آن نگاشت هر
     |     آرشیوِ دسته را به `/blog/{term}` می‌فرستاد که مسیرِ «یک نوشته» است
     |     ⇒ ۴۰۴ِ قطعی، آن هم برای پرلینک‌ترین صفحاتِ سایتِ قدیمی.
     |   • `'/weblog/' => '/blog/'` — اسلاگِ نوشته‌ها روی `[a-z0-9-]+` قید
     |     دارد، پس هر اسلاگِ فارسیِ وردپرسی هم ۴۰۴ می‌شد.
     |
     | بدتر از خودِ ۴۰۴: `TrackNotFound` آن را با آدرسِ **جدید** ثبت می‌کند،
     | پس همان سیگنالی که قرار بود نقشه از رویش ساخته شود هم پاک می‌شد.
     |
     | روشِ درست: چند هفته با `unknown => 'same-path'` بالا بماند، بعد
     | پرتکرارترین ۴۰۴ها را در `/admin/errors` ببینید و **همان‌ها** را —
     | با مقصدِ راستی‌آزمایی‌شده — این‌جا اضافه کنید.
     |
     | نکتهٔ نحوی: اگر مقصد با «/» تمام شود، دُمِ مسیر چسبانده می‌شود
     | (`'/weblog/' => '/blog/'` ⇒ `/weblog/x` → `/blog/x`). اگر نه، همه روی
     | همان یک صفحه جمع می‌شوند (`'/category/' => '/blog'` ⇒ `/blog`).
     */
    'prefix' => [
    ],

    /*
     | مسیری که در هیچ نگاشتی نیست چه شود؟
     |
     |   'same-path' (پیش‌فرض) → همان مسیر روی دامنهٔ تازه.
     |   'home'                → همه به صفحهٔ اول.
     |
     | 🔴 `same-path` عمداً پیش‌فرض است. نگاشتِ ۱:۱ برای بخش‌هایی مثل `/blog/*`
     | که اسلاگ‌هایشان یکی است رایگان کار می‌کند، و مسیرِ واقعاً ناموجود یک ۴۰۴
     | صادقانه می‌گیرد که `TrackNotFound` با ارجاع‌دهنده‌اش ثبت می‌کند — یعنی
     | فهرستِ کارِ ساختنِ نقشه از دلِ ترافیکِ واقعی درمی‌آید.
     |
     | فرستادنِ هر مسیرِ ناشناخته به صفحهٔ اول، «۴۰۴ نرم» می‌سازد: گوگل آن را
     | ریدایرکتِ بی‌ربط می‌شمارد، اعتباری منتقل نمی‌شود، و آن سیگنالِ تشخیص هم
     | برای همیشه از بین می‌رود.
     */
    'unknown' => 'same-path',

];
