<?php

use App\Services\Calendar\Providers\ContentPostProvider;
use App\Services\Calendar\Providers\DomainRenewalProvider;
use App\Services\Calendar\Providers\HostingRenewalProvider;
use App\Services\Calendar\Providers\ManualEventProvider;
use App\Services\Calendar\Providers\PaymentDueProvider;

/*
|--------------------------------------------------------------------------
| تقویم کسب‌وکار — منبعِ یگانهٔ لایه‌ها
|--------------------------------------------------------------------------
|
| درسِ `config/billing.php` این‌جا هم اعمال شده: دوره‌های صورت‌حساب یک‌بار در
| ۷ جا تکرار شده بودند و افزودنِ «شش‌ماهه» در یکی از آن‌ها جا افتاد، پس کرونِ
| تمدید سرویسِ شش‌ماهه را **هرگز** فاکتور نکرد. لایه‌های تقویم دقیقاً همان
| ریسک را دارند — کلیدشان هم‌زمان در مهاجرت، اعتبارسنجی، CSS، آیکون، فیلترِ
| جاوااسکریپت و ذخیرهٔ ترجیحِ کاربر ظاهر می‌شود.
|
| پس **فقط همین فایل**. مدل و کنترلر و Blade و JS همه از این‌جا می‌خوانند؛
| افزودنِ لایهٔ تازه یعنی یک ردیفِ تازه این‌جا (+ یک provider اگر منبع دارد).
|
| ⚠️ `provider` می‌تواند `null` باشد: یعنی لایه وجود دارد و مدیر می‌تواند
| رویدادِ دستی با آن نوع بسازد، ولی هیچ منبعِ خودکاری پشتش نیست.
*/

return [

    /*
    | ترتیبِ کلیدها = ترتیبِ نمایشِ چیپ‌ها در بالای تقویم.
    |
    | tone  → نامِ توکنِ رنگ در admin.css (`--cal-<tone>`), نه کدِ رنگ.
    | icon  → شناسهٔ اسپرایتِ `partials/icons.blade.php` (بدونِ #).
    */
    'layers' => [

        'domain_renewal' => [
            'label'    => 'تمدید دامنه',
            'hint'     => 'دامنه‌هایی که تاریخ انقضایشان نزدیک است',
            'tone'     => 'domain',
            'icon'     => 'i-globe',
            'provider' => DomainRenewalProvider::class,
        ],

        'hosting_renewal' => [
            'label'    => 'تمدید سرویس',
            'hint'     => 'سررسیدِ تمدیدِ هاست و سرور',
            'tone'     => 'hosting',
            'icon'     => 'i-server',
            'provider' => HostingRenewalProvider::class,
        ],

        'payment_due' => [
            'label'    => 'سررسید پرداخت',
            'hint'     => 'فاکتورهای پرداخت‌نشده',
            'tone'     => 'payment',
            'icon'     => 'i-coins',
            'provider' => PaymentDueProvider::class,
        ],

        /*
        | ⚠️ نامِ کلید `social_post` است ولی منبعش **زمان‌بندیِ انتشارِ محتوای
        | خودمان** است (بلاگ و پایگاه دانش، جدولِ `posts`). هیچ جدولی برای
        | زمان‌بندیِ شبکه‌های اجتماعی در این پروژه وجود ندارد؛ اگر روزی ساخته
        | شد، `provider` همین ردیف عوض می‌شود و بقیهٔ سیستم دست نمی‌خورد.
        | برچسبِ فارسی عمداً «انتشار محتوا» است تا رابط کاربری دروغ نگوید.
        */
        'social_post' => [
            'label'    => 'انتشار محتوا',
            'hint'     => 'مقاله‌های زمان‌بندی‌شدهٔ بلاگ و پایگاه دانش',
            'tone'     => 'content',
            'icon'     => 'i-book',
            'provider' => ContentPostProvider::class,
        ],

        'task' => [
            'label'    => 'یادآوری و کار',
            'hint'     => 'یادداشت‌هایی که خودتان اضافه می‌کنید',
            'tone'     => 'task',
            'icon'     => 'i-check',
            'provider' => ManualEventProvider::class,

            /*
             * ⚠️ تنها منبعی که رویدادِ **هر نوعی** می‌سازد.
             *
             * مدیر می‌تواند یادآوریِ دستی با نوعِ «سررسید پرداخت» بسازد (اجارهٔ
             * دفتر) و آن باید زیرِ چیپِ پرداخت دیده و خاموش شود، نه زیرِ این
             * چیپ — وگرنه رنگش یک چیز می‌گفت و چیپش چیزِ دیگر.
             * `CalendarService::events()` این پرچم را می‌خوانَد.
             */
            'emits'    => 'any',
        ],
    ],

    /*
    | وضعیتِ رویدادِ دستی. کلیدها در دیتابیس می‌نشینند، پس تغییرشان مهاجرت
    | می‌خواهد؛ برچسب‌ها آزادند.
    */
    'statuses' => [
        'pending'   => 'در انتظار',
        'done'      => 'انجام شد',
        'cancelled' => 'لغو شد',
    ],

    /*
    | قاعده‌های تکرار برای رویدادِ دستی (اجارهٔ ماهانه، بیمهٔ سالانه، …).
    |
    | ⚠️ «ماهانه» و «سالانه» در تقویمِ **شمسی** گام برمی‌دارند — «پنجمِ هر ماه»
    | یعنی ۵ مرداد و ۵ شهریور، نه «۳۰ روز بعد». توضیحِ کامل در
    | `Jalali::addMonths()`.
    */
    'repeats' => [
        'none'    => 'بدون تکرار',
        'weekly'  => 'هفتگی',
        'monthly' => 'ماهانه',
        'yearly'  => 'سالانه',
    ],

    /*
    | ستونِ «رویدادهای پیش‌رو» — چند روزِ آینده را نشان می‌دهد.
    */
    'upcoming_days' => 7,

    /*
    | «نزدیک است» یعنی چند روز مانده. رویدادِ داخلِ این پنجره در رابط کاربری
    | نشانِ هشدار می‌گیرد.
    |
    | ⚠️ این فقط **نمایشی** است و هیچ پیامی برای مشتری نمی‌فرستد. یادآوریِ
    | واقعیِ مشتری کارِ `services:renew-due` و `domains:lifecycle` است؛ اگر
    | این‌جا هم بفرستیم، مشتری دو بار خبردار می‌شود.
    */
    'due_soon_days' => 3,

    /*
    | سقفِ ردیفِ هر provider در یک بازه.
    |
    | چرا لازم است: بازهٔ «نمای فهرست» می‌تواند یک ماهِ کامل باشد و روی حسابی
    | با هزاران فاکتورِ پرداخت‌نشده، یک درخواستِ JSON کلِ جدول را می‌کشد.
    | ⚠️ اگر سقف خورد، پاسخ صریح `truncated: true` می‌دهد — بریدنِ خاموش یعنی
    | صفحه‌ای که می‌گوید «چیزی نیست» در حالی که هست.
    */
    'max_events_per_layer' => 300,

    /*
    | حداکثر طولِ بازهٔ درخواستی (روز). نمای ماه ۳۱ روز است و نمای فهرست
    | حداکثر یک ماه؛ عددِ بزرگ‌تر یعنی کسی دارد از API گزارشِ سالانه می‌گیرد.
    */
    'max_range_days' => 62,

    /*
    | منطقهٔ زمانیِ نمایش. `config/app.timezone` عمداً UTC است (ثابت، نه از
    | env) و دیتابیس هم UTC می‌مانَد؛ ولی «روزِ شمسی» فقط با ساعتِ تهران معنی
    | دارد — دقیقاً همان کاری که `sdate()` در `app/helpers.php` می‌کند.
    |
    | 🔴 بی‌این، رویدادِ ساعتِ ۲۲:۰۰ تهران در تقویم یک روز عقب می‌افتاد.
    */
    'display_timezone' => 'Asia/Tehran',
];
