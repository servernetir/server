<?php

/*
|--------------------------------------------------------------------------
| پایگاه دانش — محتوای صفحه /knowledge (fa / en / tr)
|--------------------------------------------------------------------------
| مقالات فعلاً نمونه‌اند و href ندارند؛ بعد از راه‌اندازی بلاگ لینک می‌شوند.
*/

return [

    /*
    | مقالاتِ نمونه — از این‌جا خوانده **نمی‌شود**
    |
    | 🔴 شش مقالهٔ ساختگی با عنوان و توضیح و **تاریخِ جعلی** این‌جا بود که
    | وقتی بلاگ خالی بود رندر می‌شد «تا صفحه خالی نماند». روی پروداکشن
    | هرگز دیده نشد (نوشتهٔ واقعی هست)، ولی روی نصبِ تازه یا یک قطعیِ
    | دیتابیس، سایت شش مقالهٔ نداشته را تبلیغ می‌کرد.
    |
    | حالا ویو در آن حالت **هیچ** نشان نمی‌دهد. صفحهٔ خالی صادق است.
    */
    'articles' => [],

    /*
    | وبینارها — عمداً خالی
    |
    | 🔴 تا شهریور ۱۴۰۵ سه وبینارِ **ساختگی** این‌جا بود با تاریخ و ساعتِ
    | مشخص، و دکمهٔ «ثبت‌نام»شان به `href="#"` می‌رفت. یعنی روی صفحه‌ای که
    | قرار است اعتبارِ فنیِ تیم را نشان دهد، رویدادی تبلیغ می‌شد که وجود
    | نداشت — همان قاعدهٔ `/status` که عمداً عددِ آپتایمِ ساختگی نمی‌سازد.
    |
    | وبینارِ **واقعی** که داشتید، همین‌جا اضافه کنید؛ بخشِ صفحه خودش
    | برمی‌گردد (ویو با `@if(!empty(...))` بسته شده). قالبِ هر ردیف:
    | ['icon' => 'mic', 'len' => '90 min', 'fa' => ['t'=>…, 'd'=>…, 'date'=>…], 'en' => …, 'tr' => …]
    */
    'webinars' => [],

    /*
    | دسته‌های مستندات — از این‌جا خوانده **نمی‌شود**
    |
    | 🔴 شش دسته با شمارشِ سخت‌کد این‌جا بود که جمعشان «۱۶۴ مقاله» می‌شد، در
    | حالی که کلِ پایگاه دانش ۳۵ سند دارد — تقریباً پنج برابر، روی یک صفحهٔ
    | عمومی، و با انتشارِ هر سندِ تازه فقط غلط‌تر می‌شد.
    |
    | حالا `SiteController::knowledge()` همان درختی را می‌خوانَد که خودِ
    | `/docs` از آن ساخته می‌شود، پس عدد نمی‌تواند دروغ بگوید. اگر بخشِ
    | تازه‌ای لازم شد، به `config/docs.php` اضافه کن نه این‌جا.
    */
    'docs' => [],

    /* آموزش و رویدادها */
    /*
    | یادگیری — عمداً خالی
    |
    | 🔴 سه ادعای مشخص این‌جا بود که هیچ‌کدام وجود نداشتند و هیچ لینکی هم
    | نداشتند: «دورهٔ رایگان — ۱۲ جلسه ویدیویی»، «پادکست — هر دو هفته»،
    | «میتاپ DevOps تهران — میزبانی فصلی». اینها آرزو نیستند، ادعای واقعی‌اند:
    | بازدیدکننده می‌خوانَد و راهی برای اقدام ندارد.
    |
    | اگر در برنامه‌اند، همین‌جا با **لینکِ واقعی** برشان گردانید؛ بخشِ صفحه
    | خودش برمی‌گردد.
    */
    'learning' => [],
];
