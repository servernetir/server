<?php

/*
|--------------------------------------------------------------------------
| هدیهٔ تولدِ مشتری
|--------------------------------------------------------------------------
|
| تاریخِ تولد از استعلامِ ثبت‌احوال می‌آید (`customer_profiles.birth_date`)،
| نه از فرمی که خودِ کاربر پر کند. یعنی **تأییدشده** است و کسی نمی‌تواند با
| تاریخِ جعلی هدیه بگیرد — همان ۶۸ هزار تومانِ استعلامِ هویت که تا امروز فقط
| هزینه بود، این‌جا سرمایه می‌شود.
|
| 🔴 `enabled` عمداً **خاموش** است.
|
| این تنها قابلیتِ سامانه است که بدونِ هیچ رویدادِ تجاری، از جیبِ شرکت پول
| توزیع می‌کند. مبالغِ زیر پیشنهادِ اولیه‌اند و تصمیمشان مالِ کارفراست؛ تا
| وقتی این کلید (یا `Setting: birthday_enabled`) روشن نشود، فرمان می‌دود و
| هیچ ریالی جابه‌جا نمی‌کند. روشن‌کردنِ ناخواسته بدتر از نداشتنِ قابلیت است.
*/

return [

    'enabled' => env('BIRTHDAY_GIFT', false),

    /*
    | مدتِ اعتبارِ هدیه (روز).
    |
    | ⚠️ این عدد در متنِ پیامکِ تأییدشدهٔ اپراتور (`%days%`) چاپ می‌شود. اگر
    | عوضش کردی، همان لحظه واقعیت هم عوض می‌شود چون `birthday:gift` سوییپ را
    | از همین می‌خواند — ولی پیامک‌هایی که قبلاً رفته‌اند عددِ قبلی را وعده
    | داده‌اند. پس تغییرش را وسطِ یک دورهٔ فعال نده.
    */
    'expires_days' => 30,

    /*
    | فقط مشتریِ دارای سرویسِ زنده.
    |
    | بی‌این شرط، هدیه روی حساب‌های مرده و ثبت‌نام‌های نیمه‌کاره پخش می‌شود —
    | یعنی دقیقاً روی کسانی که هیچ‌وقت خرجش نمی‌کنند و فقط بدهیِ دفتری
    | می‌سازند.
    */
    'require_active_service' => true,

    /*
    | پله‌ها بر اساسِ مبلغِ فاکتورهای **پرداخت‌شدهٔ ۱۲ ماهِ گذشته** (تومان).
    |
    | 🔴 معیار «مبلغ» است نه «تعداد فاکتور» — همان استدلالِ برنامهٔ نمایندگی:
    | با معیارِ تعدادی، کسی که ماهانه می‌خرد از کسی که سالانه می‌خرد بالاتر
    | می‌نشیند، در حالی که پولِ کمتری داده.
    |
    | بلندترین پله‌ای که مشتری از آن رد شده برنده است؛ فهرست از پایین به بالا
    | مرتب می‌ماند و خودِ فرمان هم مرتبش می‌کند.
    */
    'tiers' => [
        ['min_paid_irt' => 0,          'gift_irt' => 100_000],
        ['min_paid_irt' => 5_000_000,  'gift_irt' => 300_000],
        ['min_paid_irt' => 20_000_000, 'gift_irt' => 700_000],
    ],

    /*
    | سقفِ ایمنیِ روزانه.
    |
    | اگر روزی پرس‌وجو خراب شود یا داده‌ای انبوه ایمپورت شود، این عدد جلوی
    | توزیعِ ناگهانیِ اعتبار را می‌گیرد. رد شدن از سقف = توقف + فریاد، نه
    | ادامهٔ خاموش.
    */
    'daily_cap' => 50,
];
