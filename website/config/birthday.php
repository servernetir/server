<?php

/*
|--------------------------------------------------------------------------
| هدیهٔ تولدِ مشتری — کوپنِ قفل‌شده به مشتری
|--------------------------------------------------------------------------
|
| تاریخِ تولد از استعلامِ ثبت‌احوال می‌آید (`customer_profiles.birth_date`)،
| نه از فرمی که خودِ کاربر پر کند. یعنی **تأییدشده** است و کسی نمی‌تواند با
| تاریخِ جعلی هدیه بگیرد — همان ۶۸ هزار تومانِ استعلامِ هویت که تا امروز فقط
| هزینه بود، این‌جا سرمایه می‌شود.
|
| 🔴 `enabled` عمداً **خاموش** است.
|
| این تنها قابلیتِ سامانه است که بدونِ هیچ رویدادِ تجاری پول توزیع می‌کند.
| تا این کلید (یا `Setting: birthday_enabled`) روشن نشود، فرمان می‌دود و هیچ
| کوپنی صادر نمی‌کند.
|
| ⚠️ هر سه عددِ زیر از `/admin/settings` هم قابلِ تغییرند و تنظیماتِ پنل بر
| این فایل می‌چربد:
|
|     birthday_enabled       روشن/خاموش
|     birthday_amount_irt    مبلغِ کوپن (تومان)
|     birthday_valid_hours   مدتِ اعتبار (ساعت)
|     birthday_min_invoice   حداقلِ مبلغِ فاکتور برای استفاده
*/

return [

    'enabled' => env('BIRTHDAY_GIFT', false),

    /*
    | مبلغِ کوپن به تومان.
    |
    | ⚠️ این عدد در لحظهٔ صدور روی **خودِ کوپن** ذخیره می‌شود. تغییرش
    | کوپن‌های صادرشده را عوض نمی‌کند — وگرنه مشتری‌ای که پیامکِ «۵۰۰ هزار»
    | گرفته، فردا سرِ فاکتور عددِ دیگری می‌دید.
    */
    'amount_irt' => 500_000,

    /*
    | مدتِ اعتبار به **ساعت** (نه روز).
    |
    | کارفرما ۲۴ ساعت خواست؛ واحدِ ساعتی باعث می‌شود متنِ پیامک هم دقیق باشد
    | («تا ۲۴ ساعت» به‌جای «تا ۱ روز») و پنجره از همان لحظهٔ صدور شروع شود،
    | نه از نیمه‌شب.
    */
    'valid_hours' => 24,

    /*
    | 🔴 حداقلِ مبلغِ فاکتور برای استفاده از کوپن.
    |
    | خطِ قرمزِ کارفرما «هرگز زیر بها نفروش» است. کوپنِ ۵۰۰ هزاری روی فاکتورِ
    | ۶۰۰ هزاری یعنی ۱۰۰ هزار می‌گیریم برای چیزی که چند صد هزار خرج دارد — و
    | چون تحویل موفق می‌شود، هیچ خطایی هیچ‌جا ثبت نمی‌شود.
    |
    | پیش‌فرض سه برابرِ مبلغِ کوپن است (هدیه حداکثر ۳۳٪ فاکتور). صفر یعنی
    | «هیچ حداقلی» — تصمیمِ آگاهانهٔ کارفرماست، نه پیش‌فرض.
    */
    'min_invoice_irt' => 1_500_000,

    /*
    | فقط مشتریِ دارای سرویسِ زنده.
    |
    | بی‌این شرط، کوپن روی حساب‌های مرده و ثبت‌نام‌های نیمه‌کاره پخش می‌شود —
    | یعنی دقیقاً روی کسانی که هرگز استفاده‌اش نمی‌کنند.
    */
    'require_active_service' => true,

    /*
    | سقفِ ایمنیِ روزانه.
    |
    | اگر روزی پرس‌وجو خراب شود یا داده‌ای انبوه ایمپورت شود، این عدد جلوی
    | صدورِ ناگهانیِ صدها کوپن را می‌گیرد. رد شدن از سقف = توقف + فریاد، نه
    | ادامهٔ خاموش.
    */
    'daily_cap' => 50,
];
