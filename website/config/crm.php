<?php

return [

    /*
    |--------------------------------------------------------------------------
    | هویتِ فرستنده
    |--------------------------------------------------------------------------
    | نشانیِ پستی **الزامِ قانونی** است، نه تزئین: CAN-SPAM (آمریکا) و CASL
    | (کانادا) هر دو نشانیِ فیزیکیِ معتبر را در هر پیامِ تجاری واجب می‌دانند.
    | نشانیِ جعلی اینجا از «بد به نظر رسیدن» فراتر می‌رود و نقضِ قانون است.
    */
    'from' => [
        'name'    => env('CRM_FROM_NAME', 'Ehsan Ebrahimi'),
        'email'   => env('CRM_FROM_EMAIL', 'ceo@servernet.cloud'),
        'title'   => 'Founder & CEO, ServerNet Cloud',
        'site'    => 'https://servernet.cloud/webdesign',
        'address' => 'Sefir-e Omid, No. 11, Kahrom Sahel St., Urmia, Iran',
    ],

    /*
    |--------------------------------------------------------------------------
    | سقفِ روزانه
    |--------------------------------------------------------------------------
    | ۰ یعنی آن کانال خاموش است. لینکدین و اینستاگرام عمداً ۰ هستند: ارسالِ
    | خودکار روی آن‌ها نقضِ شرایطِ پلتفرم است و اکانت را می‌سوزاند. متن آماده
    | می‌شود، انسان می‌فرستد.
    */
    'caps' => [
        'email'     => (int) env('CRM_CAP_EMAIL', 30),
        'linkedin'  => 0,
        'instagram' => 0,
    ],

    /*
    |--------------------------------------------------------------------------
    | خلبانِ خودکار
    |--------------------------------------------------------------------------
    | 🔴 پیش‌فرض **خاموش** است و این یک تصمیمِ آگاهانه است، نه فراموشی.
    |
    | خاموش: سیستم سرنخ پیدا می‌کند، سایتش را می‌خواند، پیام می‌نویسد و در پنل
    | می‌گذارد؛ ارسال با یک کلیکِ تو. روشن: خودش می‌فرستد.
    |
    | چرا این‌طور: فرستنده `ceo@servernet.cloud` است — همان دامنه‌ای که فاکتور و
    | رمزِ سرویسِ مشتریانِ فعلی از آن می‌رود. اولین ایمیلِ سردِ بد، شهرتِ همان
    | دامنه را خراب می‌کند و بازگرداندنش هفته‌ها طول می‌کشد. اول ۱۰ پیش‌نویس را
    | ببین، بعد `CRM_AUTOPILOT=true`.
    */
    'autopilot' => filter_var(env('CRM_AUTOPILOT', false), FILTER_VALIDATE_BOOL),

    /*
    |--------------------------------------------------------------------------
    | گرم‌کردنِ دامنه
    |--------------------------------------------------------------------------
    | دامنه‌ای که تا دیروز فقط ایمیلِ تراکنشی می‌فرستاد و ناگهان روزی ۳۰ ایمیلِ
    | سرد می‌فرستد، برای فیلترِ اسپم دقیقاً شبیهِ یک اکانتِ هک‌شده است. سقف از
    | روزِ اولین ارسال به‌تدریج بالا می‌رود.
    |
    | کلید = چندمین روز از شروع، مقدار = سقفِ آن روز به بعد.
    */
    'warmup' => [
        'enabled' => filter_var(env('CRM_WARMUP', true), FILTER_VALIDATE_BOOL),
        'steps'   => [1 => 4, 4 => 8, 8 => 15, 15 => 25],
    ],

    /*
    |--------------------------------------------------------------------------
    | پنجرهٔ ارسال (به وقتِ UTC)
    |--------------------------------------------------------------------------
    | امارات UTC+4 است و آخرِ هفته‌اش شنبه و یک‌شنبه. ایمیلی که ساعتِ ۳ بامدادِ
    | دوبی یا وسطِ آخرِ هفته می‌رسد، خوانده‌نشده پایینِ صندوق دفن می‌شود.
    | ۵ تا ۱۲ UTC ≈ ۹ صبح تا ۴ بعدازظهرِ دوبی.
    */
    'send_window' => [
        'from_hour' => (int) env('CRM_SEND_FROM', 5),
        'to_hour'   => (int) env('CRM_SEND_TO', 12),
        'skip_days' => [6, 7],   // ISO: شنبه و یک‌شنبه
    ],

    /*
    |--------------------------------------------------------------------------
    | صندوقِ پستیِ فروش
    |--------------------------------------------------------------------------
    | `mailer` اگر تنظیم نشود روی `crm` می‌افتد که در config/mail.php به همان
    | SMTPِ سایت برمی‌گردد مگر CRM_MAIL_* را جدا بدهی. جدا دادنش بهتر است:
    | صندوقِ فروش جدا از صندوقِ سیستم یعنی جوابِ مشتری گمِ اعلان‌ها نمی‌شود.
    |
    | IMAP برای خواندنِ جواب‌ها لازم است. بدونش، «جواب داد» هیچ‌وقت تشخیص داده
    | نمی‌شود و فالوآپِ دوم برای کسی می‌رود که همان دیروز جواب داده — بدترین
    | چیزی که می‌شود برای یک سرنخِ داغ فرستاد.
    */
    'mailer' => env('CRM_MAILER', 'crm'),

    'inbox' => [
        'host'   => env('CRM_IMAP_HOST'),
        'port'   => (int) env('CRM_IMAP_PORT', 993),
        'user'   => env('CRM_IMAP_USER', env('CRM_FROM_EMAIL', 'ceo@servernet.cloud')),
        'pass'   => env('CRM_IMAP_PASS'),
        'folder' => env('CRM_IMAP_FOLDER', 'INBOX'),
    ],

    /*
    |--------------------------------------------------------------------------
    | پکیج‌ها — برای تخمینِ ارزشِ پایپ‌لاین
    |--------------------------------------------------------------------------
    */
    'offers' => [
        'Starter €1,400'  => 1400,
        'Business €2,900' => 2900,
        'Custom €6,500+'  => 6500,
        'Audit €1,800'    => 1800,
        'Social €650/mo'  => 650 * 6,
        'Care €90/mo'     => 90 * 12,
    ],

    /*
    |--------------------------------------------------------------------------
    | کشف سرنخ
    |--------------------------------------------------------------------------
    | «نقطهٔ شیرین»: کلینیکِ موفق ولی هنوز حرفه‌ای‌سازی‌نشده. زیرِ ۴٫۳ معمولاً
    | مشکلش سایت نیست؛ بالای ۴٫۸ با هزار نظر یعنی آژانس دارد و بودجه.
    */
    'discovery' => [
        'places_key'  => env('GOOGLE_PLACES_KEY'),
        'min_rating'  => 4.3,
        'max_rating'  => 4.75,
        'min_reviews' => 50,
        'max_reviews' => 400,
        'per_run'     => (int) env('CRM_DISCOVERY_PER_RUN', 15),

        /*
        | صفحه‌هایی که برای پیدا کردنِ نشانیِ ایمیل باز می‌شوند. Places شماره
        | تلفن می‌دهد ولی ایمیل نمی‌دهد؛ ایمیل باید از خودِ سایت درآید.
        | ترتیب مهم است: اولین ایمیلِ معتبر برنده است.
        */
        'contact_paths' => ['/', '/contact', '/contact-us', '/en/contact', '/about', '/ar/contact'],

        'queries'     => [
            ['q' => 'dental clinic',    'city' => 'Dubai',     'country' => 'AE', 'vertical' => 'dental'],
            ['q' => 'aesthetic clinic', 'city' => 'Dubai',     'country' => 'AE', 'vertical' => 'aesthetic'],
            ['q' => 'dental clinic',    'city' => 'Abu Dhabi', 'country' => 'AE', 'vertical' => 'dental'],
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | خط قرمزها
    |--------------------------------------------------------------------------
    | این‌ها **بعد از** تولیدِ مدل اجرا می‌شوند. مدل هرچقدر هم خوب راهنمایی شود،
    | گاهی عدد از خودش درمی‌آورد؛ این فیلتر آخرین سد است و دور زدنی نیست.
    |
    | 🔴 هیچ‌وقت این فهرست را برای «نرخِ تبدیلِ بهتر» کوتاه نکن. ادعای بزرگِ
    | بی‌پشتوانه، از نداشتنِ عدد بدتر است: خریدارِ صنعتی می‌پرسد «چطور اندازه
    | گرفتید؟» و جوابِ نداشتن، همهٔ حرف‌های راستِ دیگر را هم زیر سؤال می‌برد.
    */
    'redlines' => [
        '/\b\d{1,3}\s*%/u',                          // «۳۵٪»
        '/\b\d+\s*x\b/iu',                           // «۳ برابر»
        '/\bguarantee[ds]?\b/iu',
        '/\blimited\s+time\b/iu',
        '/\bonly\s+\d+\s+(spots?|slots?|places?)\b/iu',
        '/\b\d+\s*%\s*(off|discount)\b/iu',
        '/\bdiscount\b/iu',
        '/\bact\s+now\b/iu',
        '/\bhurry\b/iu',
    ],

    /*
    |--------------------------------------------------------------------------
    | جایگزینِ تخفیف
    |--------------------------------------------------------------------------
    | مشکلِ مشتریِ سرد قیمت نیست، ریسک است. ریسک را برمی‌داریم، قیمت را نه.
    */
    'risk_reversal' => "If you're not happy with the first design direction, you don't pay.",

];
