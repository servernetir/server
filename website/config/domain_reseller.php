<?php

/*
|--------------------------------------------------------------------------
| برنامهٔ نمایندگی دامنه — منبعِ یگانهٔ سطح‌ها و محافظ‌ها
|--------------------------------------------------------------------------
|
| هر عددی که «چقدر تخفیف» یا «چقدر سقف» را تعیین می‌کند فقط این‌جاست. همان
| درسِ `config/billing.php`: دورهٔ شش‌ماهه در هفت جا تکرار شده بود و کرونِ
| تمدید یکی‌شان را جا انداخت، پس سرویسِ شش‌ماهه **هرگز** فاکتور نشد.
|
| ⚠️ اعدادِ پله‌ها **نقطهٔ شروع برای آزمایش‌اند، نه نتیجهٔ داده**. هنوز هیچ
| نماینده‌ای نداریم که از رفتارش پله استخراج شود. بعد از یک فصلِ فروشِ واقعی
| باید بازبینی شوند.
*/

return [

    'enabled' => env('DOMAIN_RESELLER_ENABLED', true),

    /*
    |--------------------------------------------------------------------------
    | معیارِ سطح — و چرا این یکی
    |--------------------------------------------------------------------------
    |
    | `spend` = مجموعِ تومانی که نماینده در پنجره **واقعاً پرداخت کرده**
    | (ثبت + تمدید + انتقالِ پرداخت‌شده).
    |
    | 🔴 چرا نه «تعدادِ تراکنش»: قیمتِ پسوندها ده‌ها برابر فرق دارد. با معیارِ
    | تعدادی، نماینده با ارزان‌ترین پسوند بالا می‌رود و تخفیفِ به‌دست‌آمده را
    | روی `.com` و `.ai` خرج می‌کند. این سوءنیت هم لازم ندارد — هر نمایندهٔ
    | عاقلی خودش کشفش می‌کند.
    |
    | 🔴 چرا نه «حاشیهٔ سودِ ما»: از نظرِ ضدِ بازی‌کردن بهترین معیار است، ولی
    | آستانه‌ها را باید به نماینده اعلام کنیم — و آستانهٔ حاشیه‌محور یعنی
    | نماینده می‌تواند حاشیهٔ ما را از رویش حساب کند. معیارِ «چقدر به ما
    | پرداختی» همان خاصیتِ ضدِ بازی را دارد (چون با قیمت وزن می‌خورد) بی‌آنکه
    | چیزی از اقتصادِ ما لو بدهد.
    |
    | ⚠️ بهایِ این انتخاب واقعی است و باید دیده شود: معیارِ ریالی با **تورم و
    | جهشِ ارز** خودبه‌خود بالا می‌رود و بی‌آنکه فروشی رشد کرده باشد، سطح‌ها
    | ارتقا پیدا می‌کنند. پس `spend_thresholds_reviewed_at` پایین را جدی
    | بگیر: هر بار پایهٔ قیمت‌ها معنادار جابه‌جا شد، آستانه‌ها بازبینی شوند.
    */
    'metric' => 'spend',

    /** پنجرهٔ اندازه‌گیری — چرخشی، نه سالِ تقویمی */
    'window_months' => 12,

    /** آخرین بازبینیِ انسانیِ آستانه‌ها — برای هشدارِ کهنگی در پنلِ مدیر */
    'thresholds_reviewed_at' => '2026-09-26',

    /*
    |--------------------------------------------------------------------------
    | 🔴 کفِ سخت — تنها چیزی که جلوی فروشِ زیرِ قیمتِ خرید را می‌گیرد
    |--------------------------------------------------------------------------
    |
    | حاشیهٔ سودِ دامنه در این پروژه **per-TLD** است
    | (`DomainSearch::marginFor()` و `config/services.openprovider.margin`).
    | پس یک تخفیفِ ثابتِ ۱۵٪ روی پسوندی که حاشیه‌اش ۱۰٪ است یعنی فروشِ زیرِ
    | بهای تمام‌شده — بی‌هیچ خطایی، بی‌هیچ لاگی، روی هر تراکنش.
    |
    | قیمتِ نهایی هرگز از `بهای تمام‌شده × (۱ + این درصد)` پایین‌تر نمی‌رود.
    |
    | ⚠️ و وقتی کف فعال شد، **باید دیده شود**. تخفیفی که وعده داده‌ایم ۱۵٪ و
    | چیزی که در فاکتور می‌نشیند ۴٪، اگر بی‌توضیح بماند برنامه‌ای که برای
    | وفاداری ساخته شد به سندِ بی‌اعتمادی تبدیل می‌شود — و اول از همه
    | سطح‌بالاترها که باارزش‌ترین‌اند می‌فهمند. پرچمِ `floored` روی نتیجه
    | می‌نشیند و هم در API و هم در پنلِ نماینده نشان داده می‌شود.
    */
    'min_margin_pct' => 8,

    /*
    |--------------------------------------------------------------------------
    | پله‌ها — ترتیب از پایین به بالا. `min_spend_irt` شاملِ خودش است.
    |--------------------------------------------------------------------------
    |
    | ⚠️ عمداً سه پلهٔ کسب‌شدنی (به‌علاوهٔ آغازین) و نه پنج تا: روی پایگاهِ
    | نمایندهٔ امروز — که هنوز صفر است — پنج پله آن‌قدر رقیق می‌شود که هیچ
    | پله‌ای حس نشود. اضافه‌کردنِ پله بعداً آسان است؛ برداشتنش یعنی تنزلِ
    | نماینده‌ای که سطحش را ساخته بود.
    |
    | `min_active_domains` یک **دروازه** است نه معیار: بی‌آن، یک خریدِ بزرگِ
    | یک‌باره (چند دامنهٔ گران) سطحی می‌سازد که رفتارِ پایدارِ نمایندگی پشتش
    | نیست. با آن، سطح فقط به کسی می‌رسد که واقعاً پرتفویی نگه می‌دارد.
    */
    'levels' => [
        [
            'key'                => 'starter',
            'name'               => ['fa' => 'آغازین', 'en' => 'Starter',  'tr' => 'Başlangıç'],
            'min_spend_irt'      => 0,
            'min_active_domains' => 0,
            'discount_pct'       => 0,
        ],
        [
            'key'                => 'bronze',
            'name'               => ['fa' => 'برنز',   'en' => 'Bronze',   'tr' => 'Bronz'],
            'min_spend_irt'      => 50_000_000,
            'min_active_domains' => 15,
            'discount_pct'       => 5,
        ],
        [
            'key'                => 'silver',
            'name'               => ['fa' => 'نقره‌ای', 'en' => 'Silver',   'tr' => 'Gümüş'],
            'min_spend_irt'      => 250_000_000,
            'min_active_domains' => 60,
            'discount_pct'       => 10,
        ],
        [
            'key'                => 'gold',
            'name'               => ['fa' => 'طلایی',  'en' => 'Gold',     'tr' => 'Altın'],
            'min_spend_irt'      => 1_000_000_000,
            'min_active_domains' => 200,
            'discount_pct'       => 15,
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | بازبینیِ سطح — پاداشِ فوری، جریمهٔ کُند
    |--------------------------------------------------------------------------
    |
    | بالا رفتن همان لحظه که تراکنش نشست، پایین آمدن با مهلت و حداکثر یک پله.
    | عدم‌تقارن عمدی است: پاداش باید بلافاصله حس شود تا رفتار را شکل دهد، ولی
    | جریمه باید فرصتِ جبران بدهد وگرنه نماینده را می‌پرانَد.
    |
    | ⚠️ محاسبه **روزانه** است ولی تنزل فقط بعد از مهلت اعمال می‌شود؛ وگرنه
    | نماینده هر صبح سطحِ دیگری می‌بیند و برنامه غیرقابلِ فهم می‌شود.
    */
    'promote_instantly' => true,
    'demote_grace_days' => 30,
    'demote_max_steps'  => 1,

    /*
    |--------------------------------------------------------------------------
    | 🔴 بندِ ضدِ شکارِ مشتریِ مستقیم
    |--------------------------------------------------------------------------
    |
    | تخفیفِ نمایندگی به دامنه‌ای که در این بازه مشتریِ **مستقیمِ** ما بوده
    | تعلق نمی‌گیرد. بی‌این، ساده‌ترین راهِ سودِ یک نماینده این است که مشتریانِ
    | خودِ ما را با تخفیفی که ما داده‌ایم از ما بگیرد.
    |
    | ⚠️ فروش **رد نمی‌شود** — فقط با قیمتِ خرده‌فروشی انجام می‌شود. ردکردن
    | یعنی مشتریِ نهایی هیچ دامنه‌ای نمی‌گیرد و به رقیب می‌رود؛ کسی که ضرر
    | می‌کند ما هستیم، نه نماینده.
    */
    'poaching_window_months' => 6,

    /*
    |--------------------------------------------------------------------------
    | محافظ‌های API — بندِ «توکن روی سروری که ما کنترلش نمی‌کنیم»
    |--------------------------------------------------------------------------
    */
    'limits' => [
        // سقفِ پیش‌فرضِ خرجِ روزانه از اعتبار، از راهِ API (تومان). صفر = بی‌سقف.
        // ترتیبِ اولویت: سقفِ توکن ← سقفِ مشتری ← این.
        'daily_spend_irt' => 200_000_000,

        // حداکثر سالِ ثبت/تمدید در یک درخواست
        'max_years' => 10,

        // «throttle:درخواست,دقیقه»
        'rate' => [
            'read'  => '120,1',
            'check' => '60,1',
            'write' => '20,1',
        ],

        // انقضای پیش‌فرضِ توکنِ نوشتنی که رابطِ صدور پیشنهاد می‌دهد (روز)
        'token_default_days' => 365,
    ],

    /*
    |--------------------------------------------------------------------------
    | عملیاتی که عمداً **از دسترسِ توکن بیرون‌اند**
    |--------------------------------------------------------------------------
    |
    | این‌ها با یک رشتهٔ لورفته، دامنهٔ مشتریِ نهایی را می‌ربایند — نه اینکه
    | فقط پول خرج کنند. پس فقط از پنل و با احرازِ انسانی انجام می‌شوند.
    |
    | ⚠️ فهرست مستند است تا نبودشان «عمدی» خوانده شود نه «ناقص». مستنداتِ API
    | همین آرایه را چاپ می‌کند، پس اگر روزی چیزی اضافه شد، مستندات خودبه‌خود
    | راست می‌گوید.
    */
    'panel_only_operations' => [
        'auth_code'      => 'کدِ انتقال (EPP) — کلیدِ مالکیتِ دامنه است؛ اگر از API برگردد، در لاگِ WHMCS و Cloudflare می‌نشیند.',
        'transfer_unlock'=> 'خاموش‌کردنِ قفلِ انتقال — پیش‌نیازِ بردنِ دامنه.',
        'registrant_change' => 'تغییرِ مالکِ ثبت‌شده — انتقالِ مالکیت است، نه ویرایشِ اطلاعات.',
    ],

    /*
    |--------------------------------------------------------------------------
    | افزونهٔ WHMCS
    |--------------------------------------------------------------------------
    */
    'wordpress' => [
        'plugin_slug' => 'servernet-domains',
        'version'     => '1.0.0',
        'min_wp'      => '6.0',
        'min_woo'     => '7.0',
        'min_php'     => '8.0',
    ],

    'whmcs' => [
        'module_slug' => 'servernet',
        'version'     => '1.0.0',
        'min_whmcs'   => '8.0',
        'min_php'     => '8.1',
    ],
];
