<?php

/*
|--------------------------------------------------------------------------
| ServerNet — تحویلِ خودکار (Provisioning)
|--------------------------------------------------------------------------
| تنظیماتی که رفتارِ درایورهای تحویل را عوض می‌کنند و **نباید** در کد پخش شوند.
*/

return [

    /*
    |--------------------------------------------------------------------------
    | ACL نمایندگیِ WHM
    |--------------------------------------------------------------------------
    | نامِ یک ACL که **از قبل در WHM ساخته شده باشد**
    | (WHM → Reseller Center → Edit Reseller Privileges → Save as ACL list).
    |
    | 🔴 این مقدار در کد سخت‌کد نیست چون هر نصبِ WHM نامِ ACLِ خودش را دارد، و
    | ACLِ ناموجود یعنی نماینده‌ای که وارد WHM می‌شود و **هیچ دکمه‌ای** ندارد.
    |
    | ⚠️ خالی‌گذاشتنش تحویل را نمی‌خواباند (حساب ساخته می‌شود) ولی نماینده
    | بی‌مجوز می‌مانَد و در `/admin/errors` فریاد ثبت می‌شود. پیش از فروشِ اولین
    | پکیجِ نمایندگی حتماً پرش کنید.
    |
    | ⚠️ ACLای بسازید که این‌ها را **نداشته باشد**: دسترسی به SSH/روت، مدیریتِ
    | سرویس‌های سرور، ویرایشِ فایل‌های سیستم، و «all» — نماینده مشتری است، نه
    | همکار. حداقلِ لازم: ساخت/تعلیق/حذفِ حسابِ خودش، ساختِ package، DNS، و
    | ورود به cPanelِ مشتریانِ خودش.
    */
    'reseller_acl' => env('WHM_RESELLER_ACL', ''),

    /*
    |--------------------------------------------------------------------------
    | تحویلِ خودکارِ Plesk
    |--------------------------------------------------------------------------
    | 🔴 پیش‌فرض **خاموش**. درایورِ Plesk (`PleskProvisioner`) نوشته و تست‌شده
    | است، ولی فقط با پاسخ‌های شبیه‌سازی‌شده — هیچ‌وقت به یک سرورِ Pleskِ واقعی
    | وصل نشده. XML-APIِ Plesk بینِ نسخه‌ها تفاوت دارد و «تحویلِ موفقِ دروغین»
    | بدترین حالتِ ممکن است: مشتری پول داده، ما گفته‌ایم آماده است، و پنلی
    | وجود ندارد.
    |
    | خاموش که باشد، سفارشِ Plesk به صفِ دستیِ مدیر می‌رود — کندتر، ولی صادق.
    |
    | **پیش از روشن‌کردن:** یک سرورِ Plesk در `/admin/servers` ثبت کنید، آزمونِ
    | اتصال بگیرید، و یک خریدِ آزمایشی تا انتها ببرید.
    */
    'plesk_auto' => env('PLESK_AUTO_PROVISION', false),

    /*
    |--------------------------------------------------------------------------
    | فضای بکاپ/دانلود روی Storage Boxِ هتزنر
    |--------------------------------------------------------------------------
    | تا وقتی سرورِ تازه خریده شود، پکیج‌های «هاست بکاپ» و «هاست دانلود» به‌جای
    | Proxmoxِ خودمان از Storage Boxِ هتزنر تحویل می‌شوند.
    |
    | 🔴 **`plans` عمداً خالی است و نباید با حدس پر شود.**
    | نامِ نوع‌های هتزنر (`bx11`, `bx21`, …) را از خودِ API بگیرید، نه از حافظه:
    |
    |     php artisan hetzner:storage-catalog
    |
    | آن فرمان اندازه، بهایِ تمام‌شده و قیمتِ فروشِ تومانیِ هر نوع را چاپ می‌کند.
    | نگاشتِ نبود ⇒ سفارش به صفِ **دستی** می‌رود و در ردیابِ خطا فریاد ثبت
    | می‌شود؛ هرگز به یک نوعِ پیش‌فرض سقوط نمی‌کند. نوعِ اشتباه یعنی یا فضای
    | کمتر از خریدِ مشتری، یا باکسی چند برابر گران‌تر که ضررش ماه‌ها دیده نمی‌شود.
    |
    | ⚠️ Storage Box **S3 ندارد** (FTP/SFTP/SMB/rsync/Borg/WebDAV). اگر روزی
    | پکیجی با ادعای S3 فروختید، محصولِ درستش Object Storage است نه این.
    */
    'hetzner_storage' => [

        // مکانِ ساختِ باکس — نامِ Location هتزنر (مثلِ fsn1 / hel1)
        'location' => env('HETZNER_STORAGE_LOCATION', 'fsn1'),

        // پلنِ ما → نوعِ هتزنر. با خروجیِ `hetzner:storage-catalog` پر شود.
        'plans' => [
            // 'sn_backup_1' => 'bx11',
        ],

        /*
        | ⚠️ `reachable_externally` باید روشن باشد، وگرنه باکس فقط از داخلِ
        | شبکهٔ هتزنر دیده می‌شود و مشتری از ماشینِ خودش اصلاً به آن نمی‌رسد —
        | تحویلی که ۲۰۰ می‌دهد و کار نمی‌کند.
        */
        'access' => [
            'reachable_externally' => true,
            'ssh_enabled'          => true,   // rsync/SFTP/Borg از همین می‌آید
            'webdav_enabled'       => true,
            'samba_enabled'        => false,  // فقط داخلِ شبکه معنی دارد
        ],
    ],

];
