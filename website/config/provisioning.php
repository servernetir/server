<?php

/*
|--------------------------------------------------------------------------
| ServerNet — تحویلِ خودکار (Provisioning)
|--------------------------------------------------------------------------
| تنظیماتی که رفتارِ درایورهای تحویل را عوض می‌کنند و **نباید** در کد پخش شوند.
*/

return [

    /*
    |--------------------------------------------------------------------------
    | ACL نمایندگیِ WHM
    |--------------------------------------------------------------------------
    | نامِ یک ACL که **از قبل در WHM ساخته شده باشد**
    | (WHM → Reseller Center → Edit Reseller Privileges → Save as ACL list).
    |
    | 🔴 این مقدار در کد سخت‌کد نیست چون هر نصبِ WHM نامِ ACLِ خودش را دارد، و
    | ACLِ ناموجود یعنی نماینده‌ای که وارد WHM می‌شود و **هیچ دکمه‌ای** ندارد.
    |
    | ⚠️ خالی‌گذاشتنش تحویل را نمی‌خواباند (حساب ساخته می‌شود) ولی نماینده
    | بی‌مجوز می‌مانَد و در `/admin/errors` فریاد ثبت می‌شود. پیش از فروشِ اولین
    | پکیجِ نمایندگی حتماً پرش کنید.
    |
    | ⚠️ ACLای بسازید که این‌ها را **نداشته باشد**: دسترسی به SSH/روت، مدیریتِ
    | سرویس‌های سرور، ویرایشِ فایل‌های سیستم، و «all» — نماینده مشتری است، نه
    | همکار. حداقلِ لازم: ساخت/تعلیق/حذفِ حسابِ خودش، ساختِ package، DNS، و
    | ورود به cPanelِ مشتریانِ خودش.
    */
    'reseller_acl' => env('WHM_RESELLER_ACL', ''),

    /*
    |--------------------------------------------------------------------------
    | تحویلِ خودکارِ Plesk
    |--------------------------------------------------------------------------
    | 🔴 پیش‌فرض **خاموش**. درایورِ Plesk (`PleskProvisioner`) نوشته و تست‌شده
    | است، ولی فقط با پاسخ‌های شبیه‌سازی‌شده — هیچ‌وقت به یک سرورِ Pleskِ واقعی
    | وصل نشده. XML-APIِ Plesk بینِ نسخه‌ها تفاوت دارد و «تحویلِ موفقِ دروغین»
    | بدترین حالتِ ممکن است: مشتری پول داده، ما گفته‌ایم آماده است، و پنلی
    | وجود ندارد.
    |
    | خاموش که باشد، سفارشِ Plesk به صفِ دستیِ مدیر می‌رود — کندتر، ولی صادق.
    |
    | **پیش از روشن‌کردن:** یک سرورِ Plesk در `/admin/servers` ثبت کنید، آزمونِ
    | اتصال بگیرید، و یک خریدِ آزمایشی تا انتها ببرید.
    */
    'plesk_auto' => env('PLESK_AUTO_PROVISION', false),

    /*
    |--------------------------------------------------------------------------
    | نیم‌سرورهایی که به مشتریِ هاست اعلام می‌کنیم
    |--------------------------------------------------------------------------
    | مشتری هاست می‌خرد و بلافاصله می‌پرسد «حالا روی دامنه‌ام چه بزنم؟». تا امروز
    | این عدد هیچ‌جا — نه پنل، نه ایمیلِ تحویل — نوشته نمی‌شد و جوابش فقط از راهِ
    | تیکت درمی‌آمد. این نقشه همان جواب است.
    |
    | کلید = کدِ کشورِ سرور (`servers.country`، بزرگ‌حرف). هر کشوری که کلید ندارد
    | به `default` می‌افتد.
    |
    | 🔴 اولویت با ستونِ `servers.nameservers` است؛ این‌جا فقط **پیش‌فرض** است.
    | یعنی سرورِ تازه بدونِ هیچ تنظیمی جوابِ درست می‌دهد، و سرورِ خاص را مدیر در
    | /admin/servers دستی می‌نویسد.
    |
    | ⚠️ هر مقداری که این‌جا می‌گذارید باید **واقعاً به کوئریِ DNS جواب بدهد**.
    | نیم‌سرورِ اشتباه سایتِ مشتری را می‌خواباند و خطایش هم دستِ ماست نه او. هر
    | چهار مقدارِ زیر پیش از ثبت با `nslookup <domain> <ns>` سنجیده شدند و
    | authoritative جواب دادند (ns*.servernet.ir → 85.9.108.116،
    | ns*.servernet.cloud → 65.109.176.14).
    |
    | ⚠️ حداقل دو تا: تقریباً همهٔ رجیستری‌ها کمتر از دو نیم‌سرور را رد می‌کنند.
    | `ServerNameserversAreUsableTest` همین را قفل می‌کند.
    */
    'nameservers' => [
        'IR'      => ['ns1.servernet.ir', 'ns2.servernet.ir'],
        'default' => ['ns1.servernet.cloud', 'ns2.servernet.cloud'],
    ],

    /*
    |--------------------------------------------------------------------------
    | ارائه‌دهندگانی که DNS را «پروکسی» می‌کنند
    |--------------------------------------------------------------------------
    | 🔴 بدونِ این فهرست، چراغِ وضعیتِ DNS روی سایت‌های سالم قرمز می‌شود.
    |
    | مشتری‌ای که دامنه‌اش را پشتِ کلادفلر گذاشته، رکوردِ A اش IPِ **کلادفلر** را
    | برمی‌گرداند نه IPِ ما — حتی وقتی مبدأ دقیقاً همین سرور است. یعنی از روی DNS
    | به‌تنهایی **نمی‌شود فهمید** سایت کجا سرو می‌شود.
    |
    | روی سرورِ ایران دو دامنهٔ واقعی همین حالا در این حالت‌اند
    | (namakaran-alu.com و onlinemarket24.ir، هر دو روی IPِ کلادفلر). اگر
    | «نمی‌دانم» را «خراب است» ترجمه کنیم، به مشتریِ سالم می‌گوییم سایتت پایین
    | است — دقیقاً همان تیکتی که این قابلیت برای حذفش نوشته شد، این بار با
    | اضطراب.
    |
    | مقایسه با **پسوند** انجام می‌شود، پس `derek.ns.cloudflare.com` هم می‌افتد.
    */
    'dns_proxies' => [
        'cloudflare.com',
        'arvancloud.ir',
        'arvancloud.com',
        'arvancdn.ir',
        'derakcloud.ir',
        'fastly.net',
        'akam.net',
        'akamaiedge.net',
    ],

];
