# وضعیت جاری زیرساخت ServerNet

آخرین به‌روزرسانی: ۲۰۲۶-۰۹-۲۲، پس از بررسی دسترسی Windows زیرساخت ۳ و انتشار نمایش صحیح RDP در پنل مشتری. این فایل نقطه شروع کارهای بعدی است؛ وضعیت زنده باید پیش از تغییر دوباره بررسی شود.

## نقشه پایه

- میزبان ir.servernet.cloud؛ SSH با کلید روی85.9.108.114:2222؛ Proxmox روی8006.
- vmbr0: nic0، آدرس‌های85.9.108.114/29 و85.9.108.118/29، gateway=85.9.108.113.
- vmbr1: 10.10.10.1/24، LAN مشترک بیشتر مهمان‌ها؛ خروجی عادی SNAT به85.9.108.118.
- vmbr2: شبکه جداگانه با CT113/10.20.20.1؛ میزبان IPv4 ندارد.
- CT105/10.10.10.7: Nginx Proxy Manager؛ وب عمومی .118:80/443 به آن DNAT می‌شود.
- CT113/10.10.10.20: تونل‌ها و رله‌های خروجی انتخابی.
- CT125/10.10.10.62: Xray VLESS/REALITY؛ listener TCP8443.
- موجودی کامل و نگاشت دامنه‌ها در NETWORK-AUDIT-2026-09-10.md آمده است. موارد زیر بر وضعیت تاریخی آن اولویت دارند.

## تغییرات مؤثر نسبت به ممیزی اولیه

### جلوگیری از ایندکس پنل خصوصی `flow.servernet.cloud`

- Search Console یک صفحهٔ «Indexed without content» برای ریشهٔ Flow نشان می‌داد؛ این دامنه پنل عملیاتی n8n روی VM104 (`10.10.10.6:5678`) است و محتوای عمومی سایت محسوب نمی‌شود.
- روی proxy host شمارهٔ ۱ در Nginx Proxy Manager کانتینر CT105، مقدار پایدار Advanced Configuration برابر `more_set_headers "X-Robots-Tag: noindex, nofollow, noarchive";` ثبت شد. فایل تولیدشدهٔ Nginx نیز برای اعمال فوری همگام و سرویس reload شد.
- `nginx -t` موفق بود و درخواست مستقیم به `85.9.108.118` با Host/SNI دامنه، پاسخ `200` همراه هدر دقیق `X-Robots-Tag: noindex, nofollow, noarchive` برگرداند. مسیر، NAT و فایروال تغییر نکرد.
- نسخه‌های پیش از تغییر در CT105 با پسوند `pre-seo-20260921` کنار پایگاه داده و فایل proxy host نگه داشته شده‌اند. جزئیات و rollback در `SEARCH-CONSOLE-FIX-2026-09-21.md` است.
- اعتبارسنجی اصلاح Flow در Search Console آغاز شده است. در ۲۰۲۶-۰۹-۲۲ نیز Temporary Removal با prefix برای کل `flow.servernet.cloud` و `console.servernet.cloud` ثبت شد؛ هر دو هنگام ثبت در وضعیت Processing بودند. این درخواست‌ها route، NAT، فایروال یا دسترسی کاربران را تغییر نمی‌دهند و فقط نمایش در Google Search را موقتاً متوقف می‌کنند.

### دروازهٔ GPU / Cloudflare Worker `gpu-proxy`

- تیکت `TK-260920-3738` با لاگ زندهٔ Worker تطبیق داده شد: درخواست بدون توکن به `/openapi.json` در خود Worker پاسخ `401` گرفت و درخواست بعدی با `X-SN-Token` معتبر به upstream رسید، اما پاسخ `403` از `*.salad.cloud` بازگشت.
- دسترسی مستقیم از مبدأ ایران به hostname همان نمونه نیز `403` Cloudflare/Salad داد؛ بنابراین احراز ServerNet سالم بود و شکست روی WAF سمت Salad پس از عبور از دروازه رخ می‌داد.
- تیکت `TK-260920-0550` برای Ollama نیز در لاگ همان نسخهٔ Worker الگوی یکسان داشت: درخواست authenticated به `/api/chat` از گیت عبور کرد و `403` upstream گرفت؛ مشکل مشترک به workload خاص محدود نبود.
- Worker اکنون پیش از fetch به Salad، هدر داخلی `X-SN-Token` و هدرهای هویت/کشور کلاینت (`CF-Connecting-IP`، `CF-IPCountry`، `X-Real-IP`، `X-Forwarded-For`، `True-Client-IP` و `Forwarded`) را حذف می‌کند. بازنویسی Origin/Referer و رفتار redirect دست‌نخورده است.
- نسخهٔ Worker در `۲۰۲۶-۰۹-۲۰ ۱۳:۳۴:۵۰ UTC` از API Cloudflare مستقر شد؛ secret موجود `GATE_SECRET` و route سراسری `*.servernet.cloud/*` حفظ شدند. تست بدون توکن بعد از استقرار همچنان `401` است و اعتبار نحوی Worker هنگام upload تأیید شد.
- آزمون انتهابه‌انتهای authenticated هنوز باقی است: `sn-svc-212` برای تست موقت روشن شد، ولی Salad پس از تخصیص اولیه RTX 4090 و دانلود image نمونه را از دست داد و روی `ALLOCATING 0/1` ماند؛ سپس برای جلوگیری از هزینه متوقف و `STOPPED` بودن آن تأیید شد. `sn-svc-219` نیز در زمان بررسی خاموش بود. لاگ فعالیت نشان می‌دهد هر دو سرویس نزدیک زمان ثبت تیکت توسط حساب مشتری خاموش شده‌اند. rollback با deployment پیشین `395c2eb6-1484-45f3-be7f-eeffbd58f077` یا بازنشانی نسخهٔ قبل از رکورد تغییر ممکن است.
- تکرار مشتری Ollama در `۲۰۲۶-۰۹-۲۰ ۱۸:۳۴ ایران` روی نسخهٔ جدید نیز `403` گرفت. لاگ متناظر نسخهٔ `d6d1193b-25e4-4bbc-903c-37df896f50af` با `outcome=ok` نشان می‌دهد پاسخ از subrequest بالادستی آمده است؛ Ray ورودی Worker `a3e1b6cc2f287c14` و Ray صفحهٔ Salad `a3e1b6cc3b2b319d` بود. نتیجه: حذف headerها به‌تنهایی کافی نیست، چون zone مقصد می‌تواند subrequest Worker را با `cf.worker.upstream_zone` تشخیص دهد. راه بعدی باید reverse proxy غیر Cloudflare با خروج غیرایرانی باشد یا Salad درخواست‌های zone `servernet.cloud` را allowlist کند.
- جزئیات، شواهد و محدودیت‌ها: `GPU-SALAD-WAF-FIX-2026-09-20.md`.
- از `۲۰۲۶-۰۹-۲۱` fetch مستقیم Worker به Salad با relay سراسری جایگزین شده
  است: `gpu-proxy` پس از احراز به hostname DNS-only
  `gpu-relay.servernet.cloud` روی `91.107.173.226` می‌رود و Nginx با egress
  آلمان به hostname پویا و اعتبارسنجی‌شده `*.salad.cloud` متصل می‌شود. این
  مسیر خودکار همه سرویس‌های فعلی و آینده با الگوی `g-*` را پوشش می‌دهد.
- relay بدون secret پاسخ 403 و برای مقصد نامعتبر پاسخ 400 می‌دهد. گیت Worker
  بدون توکن همچنان 401 است. درخواست relay به سرویس آماده‌نشده به پاسخ عادی
  503 Gateway Salad رسید و صفحه block قبلی دیده نشد. آزمون application-level
  باقی ماند، چون `sn-svc-219` با وجود RTX 3090 حدود ده دقیقه روی
  `CREATING 99% / 0/1` ماند و سپس برای جلوگیری از هزینه متوقف شد.
- برای جابه‌جایی relay تنها A record `gpu-relay.servernet.cloud` به IP سرور
  جدید تغییر می‌کند؛ endpoint مشتری و Worker نیاز به تغییر ندارند. جزئیات
  نصب، امنیت، persistence، اعتبارسنجی و rollback در
  `GPU-EGRESS-RELAY-2026-09-21.md` و `relay/gpu-egress/README.md` است.
- ممیزی تا `۲۰۲۶-۰۹-۲۱ ۰۶:۳۱ UTC` نشان داد هیچ Container Group فعالی در
  پروژه Salad وجود ندارد و پس از deployment جدید نیز درخواست authenticated
  مشتری به `g-*` ثبت نشده است؛ بنابراین تغییر مسیر مشتری درحال‌استفاده‌ای را
  قطع نکرده است. تست واقعی application هنوز نیازمند یک replica آماده است.
- در آزمون تکمیلی همان روز، `sn-svc-219` در تلاش دوم با RTX 3090 پس از حدود
  ده دقیقه به `RUNNING 1/1` رسید. عبور `/api/tags`، `/api/chat` غیرstream و
  `/api/chat` streaming از relay آلمان تا Ollama همگی HTTP 200 گرفتند؛ مدل
  `llama3.1:8b` و پاسخ `OK` دریافت شد و صفحه block Cloudflare دیده نشد. سپس
  سرویس برای توقف هزینه خاموش و `STOPPED 0/1` تأیید شد. تست ترکیبی hostname
  برندشدهٔ Worker با توکن همان مشتری هنوز باقی است، چون نشست مدیریت برای
  بازیابی توکن مخصوص label دوباره ورود دومرحله‌ای خواست.
- در پی ادامهٔ تیکت `TK-260920-0550` در `۲۰۲۶-۰۹-۲۱` مشخص شد image سرویس
  `sn-svc-219` برخلاف پاسخ‌های قبلی پشتیبانی هنوز
  `saladtechnologies/ollama-llama3.1-recipe:1.0.0` با Ollama `0.3.11` بود و
  pull مدل رسمی `qwen3.8:27b` را با HTTP 412 رد می‌کرد. با تأیید مالک، image
  همان Container Group به `ollama/ollama:0.34.2` تغییر کرد؛ گروه از version 1
  به version 2 رفت و replica جدید با RTX 3090 به `RUNNING 1/1` رسید. آزمون
  authenticated از hostname برندشده `/api/version` مقدار `0.34.2` داد و
  `/api/pull` برای `qwen3.8:27b` بدون خطای 412 از manifest عبور کرد و دانلود
  لایه اصلی را آغاز کرد؛ دانلود کامل مدل عمداً انجام نشد. سپس برای توقف هزینه،
  گروه خاموش و `STOPPED 0/1` تأیید شد. rollback با بازگرداندن image قبلی روی
  همین گروه ممکن است؛ داده و مدل‌های دیسک محلی هر replica موقتی‌اند. منبع
  provisioning پنل هنوز image قدیمی را دارد و پیش از جلوگیری از تکرار برای
  سفارش‌های آینده به انتشار کنترل‌شده جداگانه نیاز دارد. جزئیات در
  `GPU-OLLAMA-UPGRADE-2026-09-21.md` ثبت شده است.

### زیرساخت ۳ / ArvanCloud

- در `۲۰۲۶-۰۹-۲۲` گزارش نبود راه اتصال/کنسول برای Windows Server 2025 سرویس
  `#252` بررسی شد. نمونه `#168` روشن و دارای IPv4 عمومی بود، اما API هنگام ساخت
  credential برنگردانده و رمز ذخیره‌شده‌ای وجود نداشت. reset رمز Windows نیز
  از طرف آروان صریحاً پشتیبانی نمی‌شود؛ بنابراین بازیابی این نمونه بدون نصب
  مجدد و پاک‌شدن دیسک ممکن نیست و نصب مجدد بدون تأیید مالک انجام نشد.
- پنل مشتری در commit `3403ac6` و پروداکشن اصلاح شد: image key عددی `2025` از
  خانوادهٔ واقعی کاتالوگ به‌عنوان Windows تشخیص داده می‌شود، کاربر
  `Administrator` و فرمان RDP نمایش داده می‌شوند و نبود کنسول وب با توضیح روشن
  اعلام می‌شود. SSH/root جعلی برای Windows حذف شد. route، NAT و firewall تغییر
  نکردند. جزئیات و rollback در `ARVAN-WINDOWS-ACCESS-2026-09-22.md` است.

- ردیاب خطا در `۲۰۲۶-۰۹-۲۱` دو شکست تحویل سرویس `#228` با پاسخ
  `Requested plan not found` ثبت کرد. سرویس بعداً لغو شد و در صف تحویل مورد
  بازی باقی نمانده است.
- علت ریشه‌ای: شناسهٔ flavor آروان منطقه‌ای و قابل‌تغییر است، اما مسیر تحویل
  شناسهٔ ذخیره‌شده هنگام سفارش را بدون تطبیق با `/regions/{region}/sizes`
  زنده می‌فرستاد. محافظ مشابه برای image وجود داشت ولی flavor نداشت.
- اصلاح کد در commit `f4680b3` روی `develop` منتشر و روی سایت زنده مستقر شد:
  مسیر تحویل مشخصات دقیق
  CPU/RAM/disk را به درایور می‌دهد؛ درایور ID را زنده اعتبارسنجی می‌کند و فقط
  اگر دقیقاً یک flavor کاملاً هم‌مشخصات باشد ID تازه را جایگزین می‌کند. نبود یا
  ابهام تطبیق پیش از هر POST متوقف می‌شود تا محصول یا هزینهٔ دیگری خریداری نشود.
- محافظ دوم در commit `6911407` پاسخ `Requested plan not found` را خطای همان
  ردیف می‌شناسد و فقط همان plan/location را قرنطینه می‌کند؛ بنابراین مشتری
  بعدی همان شکست را نمی‌خرد و سایر پلن‌ها/شهرها بسته نمی‌شوند.
- syntax هر دو فایل روی PHP 8.4 پروداکشن سالم بود، cacheهای Laravel پاک شدند
  و guard با یک پلن زنده و خود سرویس `#228` به‌صورت فقط‌خواندنی علیه API آروان
  موفق شد. صف تحویل پس از انتشار صفر و سلامت تحویل ابری سبز بود. هیچ خرید
  آزمایشی انجام نشد. backupهای استقرار در
  `/home/servernetcloud/deploy-arvan-flavor/backup-20260921-131601` و
  `/home/servernetcloud/deploy-arvan-flavor/backup-20260921-132055` هستند.

### VM119 / 10.10.10.65 / sn-810532.servernet.cloud

- خروجی TCP/HTTPS به سرور مشخص91.107.173.226 متصل است؛ دو سرویس تشخیص IP از داخل VM همین آدرس و loc=DE را تأیید کردند.
- مسیر: host connmark + table50 → CT113 → table119/tun119 → SOCKS10819 → SSH ثابت به91.107.173.226.
- در میزبان country-local.routes و split.state برای .65 مقدار119 دارند؛ برای .58/.99 همچنان de.
- CT113 سرویس‌های vm119-fixed-relay و servernet-country@119 و vm119-return-route را enabled/active دارد.
- مسیر برگشت CT113: 10.10.10.65/32 via10.10.10.1؛ برای تقارن conntrack ضروری است.
- DNSOverTLS ماشین119 فعال و با drop-in شبکه پایدار است. مسیر SSH از UDP عمومی پشتیبانی نمی‌کند.
- VM109 در مرحله بعد طبق بخش زیر به همین رله ثابت اضافه شد؛ نام vm119-fixed-relay اکنون یک وابستگی مشترک است.
- جزئیات و تاریخچه در VM119-GERMANY-EGRESS-2026-09-10.md.

### VLESS عمومی

- قانون فعال بازیابی شد: TCP85.9.108.118:2087 →10.10.10.62:8443.
- MASQUERADE مقصد10.10.10.62:8443 همان قانون قبلی است.
- vless-dnat.sh با انتظار15ثانیه برای قفل iptables، خروج ناموفق در خطا و بررسی نهایی قواعد اصلاح شد.
- vless-dnat.service پس از netfilter-persistent اجرا می‌شود؛ Restart=on-failure با تأخیر5ثانیه دارد.
- لینک، UUID و تنظیمات Xray تغییر نکرده‌اند.
- تست واقعی VLESS/REALITY با ورودی عمومی از داخل زیرساخت: اینترنت HTTP200، پنل10.10.10.1:8006 HTTP200 و VM119:5678 HTTP200.
- تست از دستگاه بیرونی کاربر هنوز نیاز به تأیید او دارد. reboot انجام نشده است.
- جزئیات در VLESS-RECOVERY-2026-09-10.md.

## مسائل باز و محدودیت شناخت

### هسته خروج آلمان / `91.107.173.226`

- بررسی زنده تأیید کرد این Hetzner VM هم‌زمان دو نقش تولیدی دارد: Marzban/Xray مستقیم روی TCP/UDP `1080` و مقصد SSH خروج ثابت برای VM109 و VM119 از طریق CT113.
- پنل Marzban از `sn-288838.servernet.cloud` روی Nginx `80/443` به backend محلی `127.0.0.1:8000` می‌رسد. سرور WHM/cPanel ندارد.
- ماشین Ubuntu 26.04 با ۲ vCPU، حدود ۳.۷GiB RAM، بدون Swap و دیسک ۳۸G/۳۰G آزاد است. reboot لازم و ۴۳ بسته قابل ارتقا بود؛ هیچ تغییر یا reboot انجام نشد.
- کانتینر `marzban-marzban-1` با host network و restart=always اجرا می‌شود؛ health-check و محدودیت CPU/RAM ندارد. ۱۰ کاربر active در DB ثبت بود.
- UFW غیرفعال، INPUT برابر ACCEPT و ورود root با password و key مجاز است. Fail2ban حمله‌های SSH فراوان ثبت کرده است. `.env` و DB با mode `0644` هستند و backup اختصاصی Marzban پیدا نشد.
- کنسول Hetzner در `۲۰۲۶-۰۹-۱۳` تأیید کرد هیچ Cloud Firewall به Gateway متصل نیست و Backup ارائه‌دهنده نیز فعال نیست. پلن سرور `20 TB` ترافیک خروجیِ شامل‌شده نشان می‌دهد، نه ترافیک نامحدود؛ مصرف فروش باید پایش شود. پیش از اعمال Firewall باید پورت‌های `22/tcp`، `80/tcp`، `443/tcp`، `1080/tcp+udp` و `2022/tcp` و مسیر CT113 حفظ و آزمون شوند.
- برای کاهش ریسک پیش از کار ذخیره‌سازی، در `۲۰۲۶-۰۹-۱۳` یک backup سازگار SQLite از DB زنده Marzban و یک archive محدود از تنظیمات Marzban/Nginx/SSH/Fail2ban در مسیر root-only محلی ساخته و integrity دیتابیس تأیید شد. snapshot ارائه‌دهنده و آزمون restore همچنان انجام نشده‌اند.
- rclone رسمی `1.75.1` پس از تطبیق checksum ناشر در `/usr/local/bin/rclone` نصب شد. کاربر و مسیرهای ایزوله `servernet-gateway` ساخته شدند و کد درگاه در `/opt/servernet-gateway` قرار گرفت.
- شش واحد systemd درگاه SFTP/WebDAV/S3/API و reconcile ثبت شده و پنج واحد ماندگار مربوط به سرویس‌ها/تایمر `enabled/active` هستند. SFTP روی `*:2022` می‌شنود؛ API/WebDAV/S3 فقط روی `127.0.0.1:9080/9081/9082` هستند و از Nginx روی HTTPS منتشر می‌شوند.
- چهار رکورد صریح DNS به `91.107.173.226` ساخته شد. هر چهار رکورد DNS-only هستند: پروکسی استاندارد Cloudflare پورت `2022` را عبور نمی‌دهد و پلن Free درخواست‌های بزرگ WebDAV/S3 را محدود می‌کند. مسیر Worker عمومی `*.servernet.cloud/*` تغییر نکرد؛ استثنای موقت API پس از DNS-only شدن حذف شد.
- گواهی Let's Encrypt مشترک چهار نام تا `۲۰۲۶-۱۲-۱۲` معتبر است و تمدید زمان‌بندی‌شده Certbot فعال است. API عمومی پشت TLS و rate limit با HMAC/timestamp/nonce احراز می‌شود و allowlist ثابت IP ندارد. داده مشتری مستقیماً از این سرور به Google Drive می‌رود و از Proxmox عبور نمی‌کند.
- تست‌های Python روی خود سرور ۷ مورد موفق بودند و `systemd-analyze verify` پس از اصلاح مسیر rclone خطای مربوط به واحدهای جدید نداشت. Nginx/Docker/Fail2ban/SSH، کانتینر Marzban و backend محلی پنل پس از تغییر سالم ماندند.
- OAuth Desktop Client اختصاصی ایجاد و Google Drive API فعال شد. دو حساب متصل هر کدام ظرفیت واقعی `5 TiB` گزارش کردند؛ pool مجموع `10 TiB` را با سیاست نوشتن روی upstream دارای فضای آزاد بیشتر ارائه می‌دهد.
- لایه `poolcrypt` نام و محتوای داده را پیش از ارسال رمز می‌کند. کلید بازیابی در فایل root-only محلی نگهداری می‌شود و هنوز نسخه خارج از میزبان ندارد. آزمون نوشتن/خواندن رمز‌شده، checksum و حذف دقیق داده آزمایشی موفق بود.
- برنامه OAuth در حالت Production قرار گرفت و هر دو حساب پس از انتشار دوباره authorize شدند؛ توکن‌های Production پس از آزمون جداگانه و آزمون pool رمز‌شده به‌صورت اتمی فعال شدند. نسخه Testing در snapshot محلی root-only نگهداری شده است.
- آزمون انتهابه‌انتهای مستأجر موقت موفق بود: نوشتن از هرکدام از SFTP/WebDAV/S3 و خواندن بایت‌به‌بایت از دو پروتکل دیگر، تعلیق/فعال‌سازی و پاک‌سازی داده. ایراد CRLF نسخه منتقل‌شده `auth_proxy.py` کشف و روی میزبان به LF اصلاح شد؛ مخزن برای Python اجبار `eol=lf` دارد.
- S3 به علت وضعیت experimental در rclone فقط باید به‌صورت beta عرضه شود. نسخه خارج از میزبان کلید crypt، آزمون restore، snapshot ارائه‌دهنده و پیکربندی محصول/سرور در پنل هنوز پیش‌نیاز شروع فروش هستند.
- اتصال پنل فروش در commit `4c202b5` اصلاح و روی سایت اصلی منتشر شد: محصول rclone فقط Gateway متصلِ خودش را به‌عنوان مقصد می‌گیرد، محصول بدون نیاز به دامنه واقعاً بدون دامنه سفارش می‌شود و ساخت/ویرایش آن دیگر package بی‌ربط روی WHM نمی‌سازد. lint و deploy محافظت‌شده موفق بود؛ نسخه بازگشت فایل‌های این انتشار در `/home/servernetcloud/deploy-rclone-backup/backup-20260913-134217` است.
- تنظیمات فروش پنل روی ظرفیت خام `10 TiB`، ذخیره احتیاطی `20%` و کف حاشیه `20%` قرار گرفت؛ بنابراین ظرفیت مبنای فروش `8 TiB` است. نسخه پیش از تغییر `.env` در `/home/servernetcloud/servernet_app/.env.pre-rclone-capacity-20260913` نگهداری می‌شود. ثبت رکورد Gateway، بهای تمام‌شده واقعی، محصول عمومی و آزمون خرید نهایی هنوز انجام نشده و تا تکمیل آن‌ها فروش نباید باز شود.
- گزارش کامل، پورت‌ها، persistence، ریسک‌ها، برنامه اصلاح و محدودیت‌ها: `GERMANY-EXIT-CORE-AUDIT-2026-09-12.md`.
- رکورد دقیق آماده‌سازی، اعتبارسنجی و rollback در `GERMANY-BACKUP-GATEWAY-2026-09-13.md` آمده است.

### VM109 / Personal: خروجی آلمان

- منبع10.10.10.50 با override محلی119 از host/table50 بهCT113/table119/tun119 و رله ثابت91.107.173.226 می‌رود.
- مسیر برگشت .50/32 ازCT113 به10.10.10.1 با واحد vm109-return-route پایدار است.
- DNS منبع .50 رویTCP/UDP53 در میزبان بهCT113:53530 DNAT می‌شود؛ vm109-relay-dns آن را از SOCKS10819 با TLS بهCloudflare DNS می‌فرستد.
- واحد NAT در میزبان vm109-relay-dns-nat است. هر سه واحد جدید enabled هستند.
- ترافیک VM و DNS روی قواعد دیده شد؛ تست خود رله IP91.107.173.226 داد. تست مرورگر Windows داخلVM109 هنوز توسط کاربر لازم است؛ Guest Agent ندارد.
- جزئیات در VM109-GERMANY-RECOVERY-2026-09-10.md.

### رله معرفی‌شده به‌عنوان ترکیه و پنل

- مشخصات رله: ehsan@89.251.8.123 رویSSH822؛ کاربر کلید اختصاصی CT113 را اضافه کرد و ورود کلیدی موفق شد.
- در CT113، servernet-turkey-relay و servernet-country@tr فعال و enabled شدند؛ SOCKS محلی10822، tuntr و table190 با default و blackhole پشتیبان. هیچ VM به آن تخصیص داده نشده و گزینه پنل اضافه نشده است.
- دو درخواست HTTPS از SOCKS، IP89.251.8.123 را تأیید کردند؛ Cloudflare مقدار loc=BG داد، نه TR. این مشاهده تعیین قطعی محل فیزیکی نیست؛ تا رفع/تأیید اختلاف نباید خروجی ترکیه تأییدشده معرفی شود. جزئیات و rollback در TURKEY-RELAY-2026-09-10.md.
- صفحه واقعی مدیریت پس از ورود بررسی شد: شش رکورد ثبت‌شده، نه همه مهمان‌های زنده؛ VM109/116/119 در فهرست نبودند. heartbeat هر دو agent تازه بود. برخی وضعیت‌ها و نگاشت‌های تاریخی با زیرساخت زنده ناسازگارند؛ اصلاح خودکار نشده‌اند.
- برنامه مبتنی بر کد موجود پنل در EXIT-INFRA-IMPLEMENTATION-PLAN.md ثبت شده؛ توسعه/استقرار قابلیت جدید پنل در این مرحله انجام نشده است.

### VM116 / Selmi: بازیابی‌شده

- net0 به MAC سالم قبلی BC:24:11:95:E1:6C رویvmbr1 بازگردانده شد؛10.10.10.61 اکنون پاسخ‌گو است. MAC جایگزین51:A8:5A باعث قطع شبکه شده بود؛ عامل تغییر مشخص نیست.
- DNAT عمومی85.9.108.118:20000 →10.10.10.61:22 و MASQUERADE مربوط فعال هستند.
- vm116-ssh-dnat.sh مانند VLESS، انتظار قفل، خروج روی خطا و بررسی نهایی دارد؛ واحد بوت پس از netfilter-persistent اجرا و در خطا retry می‌شود.
- handshake از ویندوز بیرونی به پورت20000، با fingerprint برابر مهمان داخلی موفق شد. ورود با حساب مشتری تست نشده است.
- VM reboot نشد. جزئیات در VM116-SSH-RECOVERY-2026-09-10.md.

### موارد باقی‌مانده

- Gate/CT111 و Pritunl/CT114 همچنان IP تکراری10.10.10.12 دارند.
- فایروال Proxmox در ممیزی خاموش و IPv4 میزبان ACCEPT بود؛ اصلاح نشده است.
- اشکال NAT و MAC ماشین116 طبق بخش بالا رفع شد؛ منشأ تغییر قبلی MAC هنوز مشخص نیست.
- Jitsi:443 در ممیزی اتصال را رد می‌کرد؛ backendهای .64 به VM خاموش اشاره داشتند؛ اصلاح نشده‌اند.
- داخل۱۰ VM بدون Agent/دسترسی معتبر و فایروال دیتاسنتر به‌طور کامل قابل بازرسی نبود.
- country-agent ورودی محلی را با پاسخ پنل ادغام می‌کند؛ پاسخ خالی/نامعتبر پنل مانند منطق قبلی باعث خروج زودهنگام می‌شود.
- فایل‌های rules.v4/v6 با کل وضعیت زنده برابر نیستند؛ persistence ترکیبی از فایل‌ها، unitها و agentهاست.

## توسعه یکپارچه پنل — ۲۰۲۶-۰۹-۱۰

- کاربر استفاده از IP89.251.8.123 با نام ترکیه را با اطلاع از تشخیصBG پذیرفت؛ تفاوت نام/کشور مشاهده‌شده حفظ شود.
- بازبینی زنده: ۱۴ VM غیرقالب، ۷ قالب و۷ کانتینر؛ سرویس‌های رله DE وTR و قواعد .50/.65 به119 برقرار بودند. این مرحله شبکه زنده را تغییر نداد.
- برنامه اجرایی جدید و معیارهای پذیرش در EXIT-INFRA-ROADMAP.md؛ طرح قبلی سند زمینه است.
- نخستین تغییر محلی پنل: موجودی فقط‌خواندنی QEMU/LXC، بدون import خودکار و بدون ارسال سیاست جدید به عامل‌ها. هنوز deploy نشده است.
- گزارش تحویل و rollback: EXIT-INFRA-PHASE-A-2026-09-10.md؛72 تست مرتبط با236 assertion موفق شدند. تخصیص مشتری و عامل شبکه یکپارچه مراحل بعد هستند.
- شواهد کد: ابزار Ansible کشورهای اختصاصی هنوز CT115 را sync می‌کند؛ اتصال کامل CRUD upstream به agent در کد فعلی پیدا نشد. اجرای کور نقش‌ها ممنوع؛ این شکاف‌ها در roadmap ثبت شدند.

## روال ثبت تغییرات

پس از هر تغییر شبکه، این فایل را به‌روز کنید؛ شواهد، فایل‌های تغییرکرده، تست‌ها، محدودیت‌ها و راه بازگشت را در گزارش مربوط ثبت کنید. اعتبارنامه‌ها در مستندات ثبت نشوند.
