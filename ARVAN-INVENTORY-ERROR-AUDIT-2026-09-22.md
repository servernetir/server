# ممیزی موجودی آروان و ردیاب خطا — ۲۰۲۶-۰۹-۲۲

## تکمیل عملیات — ۲۰۲۶-۰۹-۲۳

این بخش بر محدودیت‌های تاریخی حذف در پایین سند مقدم است.

- با تأیید صریح مالک، پنج ماشین مربوط به سرویس‌های لغوشده `#233`، `#234`،
  `#244` و دو نسخهٔ اضافهٔ `sn-svc-233` و `sn-svc-254` حذف شدند. پاسخ‌های حذف
  موفق و inventory پس از عملیات شامل دو ماشین متصل، صفر orphan و صفر ghost بود.
- دو ماشین حفظ‌شده `#241` و `#254` هرکدام نرخ ساعتی ۱۵۰۰ تومان و کسر تازهٔ
  `cloud_hourly` در دفتر کیف پول داشتند. بررسی درآمد باید دفتر کیف پول را
  نیز شامل شود؛ صرف شمارش فاکتورهای پرداخت‌شده برای این محصول نادرست است.
- `#253` در پنل آروان ابتدا soft_deleted بود. حذف صریح همان شناسه پذیرفته شد؛
  فهرست پس از تازه‌سازی دیگر آن را نشان نداد. بررسی بعدی دیتابیس زنده:
  service cancelled/provision none؛ instance deleted/last_error null.
- سرور تست Windows 2025 با حساب شخصی مالک، سرویس `#257`، ساخته شد. پنل
  Administrator و فرمان RDP را نشان داد، اما رمز موجود نبود. آخرین وضعیت
  زندهٔ سرویس terminated/provision none است؛ آزمون ورود موفق ویندوز انجام نشد.
- حذف ماشین‌ها برگشت مستقیم ندارد؛ وجود نسخهٔ پشتیبان برای دیسک‌های حذف‌شده
  تأیید نشده است. route، NAT و firewall در این عملیات تغییر نکردند.
- مسیر `GET /ecc/v1/regions/{region}/servers/{id}/vnc` علیه حساب واقعی برای
  ماشین زنده پاسخ HTTP 200 با URL کنسول داد. فرض پیشینِ نبود API رد شد؛
  قابلیت نمایش پنل تا آزمون کامل و استقرار کد، تکمیل‌شده محسوب نمی‌شود.

## دامنه و روش

- فهرست زندهٔ تمام regionهای قابل مشاهدهٔ حساب آروان با رکوردهای
  `cloud_instances`، سرویس و کد مشتری در پروداکشن تطبیق شد.
- هیچ ابرک، سرویس، لاگ، route، NAT یا قانون فایروال در این مرحله حذف یا جابه‌جا
  نشد. اعتبارنامه و شناسهٔ کامل مشتری در این سند ثبت نشده است.

## یافتهٔ موجودی

گزارش قدیمی هر ۷ ابرک را «متصل» می‌دانست، اما چهار ردیف پشت سرویس لغوشده بود و
دو ماشین `sn-svc-254` به یک سرویس نسبت داده می‌شدند. علت، fallback تطبیق hostname
بود که مصرف یک نمونه را رزرو نمی‌کرد.

پس از commit `5339228` نتیجهٔ زنده چنین است:

- ۵ ماشین متصل، ۲ ماشین بی‌مالک و ۰ سرویس شبح.
- سرویس‌های زنده و سالم: `#241` و `#254`؛ ماشین دارای provider-ref ثبت‌شدهٔ
  `#254` همان نسخهٔ معتبر است.
- ماشین‌های پشت سرویس لغوشده: `#233`، `#234` و `#244`.
- ماشین‌های بی‌مالک: نسخهٔ دوم `sn-svc-233` و نسخهٔ دوم `sn-svc-254`.
- `#253` خارج از فهرست عادی inventory ولی در صف release-retry است؛ API حذف را
  با «نمونه برای اقدام آماده نیست» موقتاً رد می‌کند.

## اصلاحات کد و انتشار

- `CloudInventory` ابتدا همهٔ refهای زندهٔ provider را می‌بیند؛ اگر ref واقعی
  نمونه حاضر باشد، ماشین هم‌نام دیگر حق fallback ندارد.
- هر `CloudInstance` در هر دور فقط یک بار claim می‌شود؛ duplicate بعدی orphan
  است، مستقل از ترتیب پاسخ API.
- تمام نمایش‌های شناسهٔ سرویس در صفحهٔ موجودی برای مقدار تهی fallback دارند؛
  خطای ثبت‌شدهٔ `fa_num(null)` دیگر صفحه را ۵۰۰ نمی‌کند.
- regression test حالت «duplicate قبل از ref واقعی» اضافه شد.
- commit `5339228` روی `develop` push و دو فایل runtime با backup محافظت‌شده روی
  پروداکشن منتشر شد. PHP 8.4 lint، `view:clear` و `view:cache` موفق بودند.

## ردیاب خطا

- خطای صفحهٔ inventory رفع شد.
- اجرای دستی `fleet:scan` موفق بود: ۴۳ دیده‌شده، بدون خطای provider؛ شکست‌های
  زمان‌بند قبلی گذرا بوده‌اند و در اجرای جاری تکرار نشدند.
- سلامت جاری OpenProvider موجودی را با موفقیت می‌خواند؛ رخدادهای قدیمی token و
  EUR در ردیاب، وضعیت جاری را بازتاب نمی‌دهند.
- رخدادهای `unknown_template` برای n8n مربوط به templateهای تعریف‌نشدهٔ سامانهٔ
  پیامک است و بدون افزودن template در مقصد نباید با retry بی‌نهایت حل شود.
- خطای فعال باقی‌مانده، release سرویس `#253` و نشتی پنج ماشین مشخص‌شده در بالا
  است. پاک‌کردن رکوردهای تاریخی ردیاب عمداً انجام نشد تا شواهد باقی بماند.

## نام مکان‌های آروان

برای regionهای آروان دو location-code هم‌زمان دیده شد: نام قدیمی شهر و نام جدید
کدنیم ServerNet (Simin/Tehran، Foroogh/Isfahan، Bamdad/Shiraz،
Shahriar/Urmia، Qeysar/Ahvaz). provider_location واقعی عوض نشده و provisioning
همچنان region درست را می‌فرستد، ولی سینک می‌تواند نام قدیمی را دوباره بسازد.
canonicalization این نگاشت باید در تغییر بعدی مستقل اصلاح و سپس `cloud:sync`
کنترل‌شده اجرا شود؛ اجرای کور سینک ممنوع است.

## محدودیت و rollback

- حذف پنج ماشین و `#253` هنوز انجام نشده و مستلزم تأیید دقیق اقدام تخریبی است.
- rollback کد: برگرداندن دو فایل از
  `/home/servernetcloud/deploy-inventory-5339228/backup` و سپس اجرای
  `view:clear` و `view:cache` با PHP 8.4.
- این تغییر هیچ اثر شبکه‌ای ندارد؛ مسیرها، NAT و firewall دست‌نخورده‌اند.
